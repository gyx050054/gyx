# 智能灌溉系统 第二版用户需求文档

> 文档版本：v2.0（2026-08-05）
> 与《第二版设计方案.md》配套：本文件从**用户视角**描述功能需求，设计/技术方案见另一份。

---

## 1. 产品概述

本系统是一个面向**多农户、多租户**的农田灌溉管理平台，定位是**让不会用 ThingsBoard 的人也能方便地用上物联网灌溉**。

第一版只能一个账号登录、田块设备都是后台脚本建好的；第二版让用户**自己注册账号、自己建田块、自己加设备**，并支持多个互不干扰的租户（每个租户=一个农户/一个种植基地，数据完全隔离）。

核心承诺（用户可感知）：
- **注册即用**：不需要懂任何物联网平台概念，注册后马上能建田块、建设备
- **中文、傻瓜式操作**：所有操作都是"田块""设备""开关""定时"这类日常词
- **数据隔离**：不同租户之间互相看不到对方任何数据

---

## 2. 用户角色

| 角色 | 说明 | 谁能操作 |
|------|------|---------|
| **平台运营者** | 系统所有者，管理平台本身（一般不用 App） | 内部使用 |
| **租户管理员** | 一个农户/基地的主人，注册获得 | 管理本租户全部内容 |
| **农户（客户用户）** | 租户管理员创建的子账号，如家庭其他成员、多个地块负责人 | 查看数据、控制设备、设定时任务 |

**一句话**：租户管理员是"老板"，农户是"员工"。老板建田块、建设备、给员工开账号；员工干活（开关阀、设任务）。

---

## 3. 功能需求（用户视角）

### 3.1 注册

**3.1.1 租户注册（App 首页入口）**

- 打开 App 首先看到登录页，登录页上有"注册"入口
- 用户点击注册，填写：邮箱（即登录账号）、密码（+确认密码）
- 注册成功后系统自动创建：该用户的租户 + 该用户的租户管理员账号
- 注册成功提示"注册成功，请登录"，用户即可用邮箱密码登录
- 邮箱格式校验：不是合法邮箱不给注册；邮箱重复不给注册
- 注册失败有明确错误提示

> 实现说明：注册请求发到微服务端，由系统后台完成创建（用户无感，无需邮件激活）。

**3.1.2 农户账号（租户管理员在 App 内创建）**

- 租户管理员在"农户管理"页面点击"新增农户"
- 填写：农户名称、邮箱、初始密码
- 创建成功后，该农户用邮箱密码即可登录
- 农户名称和邮箱在租户内可管理（列表查看、删除）

**3.1.3 预置演示账号（模拟器数据专用）**

- 系统预置一个演示用租户管理员账号：`15079983758@163.com`，初始密码 `147258`
- 该账号下预置完整演示数据：9 块田块 + 27 台模拟器设备（电动阀/温度湿度计），登录即可看到全部数据，无需自己创建
- 用途：租户管理员在 App 里新增设备/田块时，**模拟器不会跟着新增**（模拟器设备是脚本预置的）；演示/验收时用本账号即可看到田块与设备数据
- 该账号首次登录同样强制改密（见 3.2）

### 3.2 登录

- 登录页保留邮箱+密码登录（第一版已实现，继续沿用）
- 登录后系统自动识别身份：
  - **租户管理员** → 进入管理视图（见 3.3）
  - **农户** → 进入使用视图（见 3.3）
- ~~登录状态保存~~（未实现）：TokenStore 已存 token，但关闭 App 再打开仍显示登录页，需重新登录
- **首次登录强制改密**：无论注册账号、农户账号还是预置演示账号，**第一次登录必须先修改密码才能进入主界面**；预置账号初始密码 `147258` 仅首次登录有效
- 登录失败有明确错误提示

### 3.3 角色权限（登录后能看到什么）

| 功能 | 租户管理员 | 农户 |
|------|:---:|:---:|
| 田块总览（看数据） | ✅ | ✅（只看本租户） |
| 田块详情、设备控制、定时任务 | ✅ | ✅ |
| 新增/删除田块 | ✅ | ❌ |
| 新增/删除设备 | ✅ | ❌ |
| 农户账号管理 | ✅ | ❌ |
| 任务管理 | ✅（本租户全部） | ✅（本租户全部） |

**权限边界（已确认：农户可操作但不可管理）**：

- **农户能"用"**：查看数据（温湿度/阀门状态/历史曲线）、开关电动阀（单控/批量）、创建/取消定时任务——日常干活的操作都放开
- **农户不能"管"**：新增/删除田块、新增/删除设备、农户账号管理——改结构、加账号这类管理操作只归租户管理员
- 一句话：**农户管"用"，租户管理员管"管"**。表格中"田块详情、设备控制、定时任务"均指"使用"层面，不含任何增删改结构操作

**数据隔离硬性要求**：租户 A 的用户在任何页面都看不到、操作不到租户 B 的任何田块/设备/任务。跨租户数据泄露=严重缺陷。

### 3.4 田块管理（租户管理员）

**3.4.1 田块列表**

- 田块总览页以卡片/列表展示本租户全部田块（第一版已有查看能力）
- 每张卡片：田块名称、田块内设备数量

**3.4.2 新增田块**

- 田块总览页右上角"+"→ 新增田块
- 输入田块名称（必填，租户内不能重名）
- 保存后新田块立即出现在总览页（设备数为 0）

**3.4.3 删除田块**

- 田块卡片提供删除操作（长按/菜单）
- 删除前二次确认，明确提示"该田块下有 N 台设备，删除田块后设备将不再归属任何田块"
- 确认后：田块被删除，其下设备变为"未挂载"状态（设备本身不删除，可重新挂到别的田块）

### 3.5 设备管理（租户管理员）

**3.5.1 设备列表**

- 田块详情页展示该田块下设备（第一版已有）
- 新增一个"全部设备"视图：展示本租户所有设备，包括未挂载田块的设备

**3.5.2 新增设备**

- 入口：田块详情页或"全部设备"页的"+"按钮
- 表单字段：
  - **设备名称**（必填，租户内唯一）
  - **设备类型**（必选，仅两个选项：电动阀、温度湿度计）
  - **挂载田块**（必选，下拉选择本租户田块）
- 保存后设备出现在对应田块下，类型决定它的数据展示方式：
  - 电动阀：显示开关状态、电量，可控制
  - 温度湿度计：显示温度、湿度，不可控制
- 设备类型创建后**不可更改**（如需改类型，删除重建）

**3.5.3 删除设备**

- 设备卡片提供删除操作
- 删除前二次确认，明确提示"删除将取消该设备所有未完成/执行中的定时任务，且不可恢复"
- 确认后：设备删除，其未完成任务一并取消

**3.5.4 设备接入凭证（真设备连接用）**

- 新增设备成功后，自动弹出"设备凭证"页面，展示该设备的 **Access Token**（可复制）
- 提供提示文案："将 Token 配置到你的设备后，设备数据会自动出现在系统里"
- 已创建的设备也可随时在设备详情里重新查看 Token

### 3.6 真设备接入

- 用户按设备说明书/引导把 Access Token 填入设备（或扫 App 里的二维码，第二版先做手动录入，二维码后续）
- 设备联网后自动上报数据，App 无需任何额外配置即可看到实时数据
- 支持的真实硬件：能连接 WiFi/4G 并支持 MQTT 的设备（如 ESP32 网关+传感器/阀门，具体由硬件接入文档说明）
- 模拟器与真设备体验一致（现有模拟器数据可无缝过渡到真设备）

### 3.7 数据查看（沿用第一版）

- **田块总览**：所有田块卡片，显示田块名、设备数（第一版已实现）
- **田块详情**：田块下设备列表
  - 温度湿度计卡片：设备 ID、当前温度℃、当前湿度 %RH、安装田块、最近上报时间
  - 电动阀卡片：当前状态（工作中/未工作）、电量 %，工作状态下直接显示瞬时流量/累计用水量/水压（见 3.8 UI 要求）
  - 页面每 10 秒自动刷新（第一版已实现）
- **历史曲线**：点击温度湿度计进入历史页，折线图展示温度/湿度变化，支持近 1 小时/24 小时/7 天/自定义时间范围（第一版已实现）

### 3.8 电动阀控制（沿用第一版）

- **单设备控制**：电动阀卡片上的开关按钮，点击开启/关闭，操作成功/失败有明确反馈
- **批量控制**：田块详情页多选多个电动阀，一键全部开启/全部关闭
- **阀门卡片 UI 要求（第二版升级）**：
  - 卡片要**大气、好看**：醒目的大号开关控件（带渐变色的圆形/胶囊开关），状态色清晰（工作中=明亮蓝色/绿色，未工作=灰色）
  - **数据完整显示**：卡片上直接展示瞬时流量（L/min）、累计用水量（m³）、水压（MPa）、电量（%）——工作状态下随 10 秒刷新实时更新，未工作时显示最近一次值或 `--`
  - 信息层次：顶部设备名+状态徽标 → 中间大开关 → 底部数据行（流量/水压/电量），卡片整体留白充足、圆角、阴影，避免拥挤

### 3.9 定时浇水任务（沿用第一版 + 多租户隔离）

- **创建任务**：选择单个或多个电动阀，设置开始时间（默认立即）、持续时长（≥1 分钟），到点自动开、到时自动关
- **冲突检测**：同一设备同一时间段已有任务（等待/执行中）时拒绝创建，提示冲突；批量添加时任一设备冲突则全部拒绝
- **任务管理页**：展示所有任务（任务 ID、设备、开始/结束时间、状态），支持取消
  - 未开始任务：直接取消
  - 执行中任务：先暂停设备再取消
- **状态**：等待执行 / 执行中 / 已完成 / 已取消
- **多租户要求**：任务列表只显示本租户的任务（第一版是全局任务，本版修复）

### 3.10 农户管理（租户管理员）

- 页面入口：管理菜单 → "农户管理"
- 功能：新增农户（名称+邮箱+密码）、农户列表（邮箱、名称）、删除农户
- 删除农户前二次确认（提示该农户登录后将失效）

---

## 4. 与第一版的差异清单

| # | 变化 | 说明 |
|---|------|------|
| 1 | **新增：租户注册** | 第一版无注册；第二版注册即建租户+管理员 |
| 2 | **新增：多租户与双角色** | 第一版单账号；第二版租户管理员/农户两种身份，数据隔离 |
| 3 | **新增：田块增删** | 第一版田块由脚本预置；第二版管理员自助增删 |
| 4 | **新增：设备增删+挂载田块** | 第一版设备由脚本预置；第二版管理员自助建设备，可挂到任意田块 |
| 5 | **新增：设备凭证展示** | 新增设备后展示 Access Token，支撑真设备接入 |
| 6 | **修改：任务按租户隔离** | 第一版任务全局共享；第二版各租户只见自己的任务 |
| 7 | 登录持久化（部分） | 第一版重启掉线；第二版 token 本地存储已做，但重启自动登录未实现 |
| 8 | **沿用：全部第一版功能** | 田块总览、设备详情、历史曲线、单控/批量、定时任务、10 秒刷新 |
| 9 | **新增：首次登录强制改密** | 所有账号（注册/农户/预置）首次登录必须修改密码后才能进入主界面 |
| 10 | **新增：预置演示账号** | `15079983758@163.com` / `147258`，预置 9 田块 + 27 台模拟器设备，演示/验收用 |
| 11 | **修改：阀门 UI 升级** | 大号开关控件、状态色区分、卡片直接显示流量/水压/电量数据 |
| 12 | **新增：模拟器说明** | App 新增设备/田块不影响模拟器；模拟器数据由预置账号承载 |

---

## 5. 界面原型描述

（各页面交互说明，最终以 App 实现为准）

1. **登录/注册页**：Logo + 邮箱 + 密码 + 登录按钮 + "没有账号？注册"链接
2. **田块总览页**（租户管理员）：田块卡片网格 + 右上角"+"新增田块；长按田块卡片出现"删除"菜单
3. **田块详情页**：顶部田块名 + "新增设备"按钮；设备卡片列表（温度湿度计/电动阀两类）
4. **新增设备弹窗/页**：名称输入框 + 类型单选（电动阀/温度湿度计）+ 田块下拉选择 + 保存
5. **设备凭证弹窗**：Access Token 文本 + 复制按钮 + 使用说明文案
6. **农户管理页**（仅租户管理员可见）：农户列表 + "新增农户"按钮
7. **任务管理页**：任务列表 + 状态标识 + 取消按钮（沿用第一版样式）
8. **我的页面/菜单**（新增）：显示当前身份（租户管理员/农户）、退出登录；租户管理员可见"农户管理""田块管理"入口

---

*本需求为第二版完整范围。实现顺序、技术选型详见《第二版设计方案.md》。*
