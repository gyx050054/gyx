# 智能灌溉系统 第二版内部需求文档

> 版本：v2.0（2026-08-05）
> 读者：开发人员。本文档从**开发视角**描述第二版要实现什么、接口怎么定、改哪些文件、实现步骤。
> 配套：《第二版用户需求文档.md》（用户视角）、《第二版设计方案.md》（技术依据）
> 涉及代码：`task-service/`（微服务端，Spring Boot+MySQL）、`kotlin-demo/`（Android APP，Retrofit）、`物联网灌溉系统第一代第三版/simulators/`（模拟器）

---

## 1. 总体架构（第二版）

```
App（Android）
 ├─ 登录/日常操作 ──直连──▶ ThingsBoard REST（8080）  ← 保持第一版架构
 │      （田块/设备/遥测/RPC/凭证/改密 全部直连 TB）
 ├─ 注册/强制改密标记 ──▶ 微服务端（:9300）            ← 第二版新增（TB CE 4.x 无自助注册）
 └─ 任务管理 ──▶ 微服务端（:9300）
模拟器/真设备 ──MQTT──▶ ThingsBoard（1883）
微服务端 ──持 SysAdmin token──▶ ThingsBoard（建租户/用户）
```

**原则**：能直连 TB 的绝不经过微服务端中转；微服务端只负责 TB 做不到的三件事——① 注册建租户（需 SysAdmin）② 强制改密标记 ③ 定时任务（第一版已有，加租户隔离）。

---

## 2. 角色与权限（TB 模型映射）

| App 角色 | TB Authority | 权限范围 |
|---------|-------------|---------|
| 租户管理员 | `TENANT_ADMIN` | 本租户全部实体（田块/设备/客户/用户/任务），可增删改 |
| 农户 | `CUSTOMER_USER` | 仅**被分配**的田块/设备（可查看+控制，不可管理） |
| （内部）SysAdmin | `SYS_ADMIN` | 平台级，凭证只放微服务端配置 |

**实现要点**：
- 农户（Customer User）能"用"不能"管"：靠 TB 原生模型保证——田块（Asset）/设备（Device）**分配给 Customer** 后，Customer 用户天然可查看/控制（RPC），但 `POST /api/asset`、`POST /api/device` 等管理接口需要 TENANT_ADMIN 权限，Customer 调用返回 403，**无需自己写权限判断**
- App 查询接口按角色切换：
  - TENANT_ADMIN：`GET /api/tenant/deviceInfos`、`GET /api/tenant/assetInfos`（第一版已在用）
  - CUSTOMER_USER：`GET /api/customer/{customerId}/deviceInfos`、`GET /api/customer/{customerId}/assets`（customerId 从登录 JWT 的 `customerId` 字段取）
- 任务接口（微服务端）按 `tenantId` 过滤（见 §5）

**TB 侧需要做的预置（脚本）**：现有 DeviceProfile（VALVE / TEMPERATURE_HUMIDITY）、AssetProfile（FIELD）已存在，**复用，不重建**。

---

## 3. 注册与账号

### 3.1 租户注册（微服务端新增接口）

```
POST /api/auth/register            （微服务端 :9300，新增 AuthController）
body:  { "email": "...", "password": "..." }
响应:  { "success": true } | { "success": false, "message": "原因" }
```

微服务端内部（`ThingsBoardClient` 新增方法，RestTemplate 调 TB）：

```
① 用配置中的 SysAdmin token 登录 TB 拿 sysToken（POST /api/auth/login，admin@thingsboard.org）
② POST /api/tenant {title: email}                     → tenantId
③ POST /api/user?sendActivationMail=false
     {email, authority:"TENANT_ADMIN", tenantId}        → userId（新用户默认未激活）
④ GET  /api/user/{userId}/activationLinkInfo            → activateToken
⑤ POST /api/noauth/activate {activateToken, password}   → 设置用户填的密码（返回 JWT，丢弃即可）
⑥ 微服务端记录 must_change_password=true（见 3.3）
```

**前置安全要求**：SysAdmin 账号 `admin@thingsboard.org` 的**默认密码必须先改**（默认 sysadmin 全网公开）。`task-service/src/main/resources/application.yml` 增加配置：`thingsboard.sysadmin.username` / `thingsboard.sysadmin.password`。

### 3.2 农户账号（App 直连 TB，租户管理员操作）

```
① POST /api/customer {title: 农户名称}                          → customerId
② POST /api/user?sendActivationMail=false
     {email, authority:"CUSTOMER_USER", customerId}              → userId
③ GET  /api/user/{userId}/activationLinkInfo → POST /api/noauth/activate
④ 把需要访问的田块/设备分配给该客户：
     POST /api/customer/{customerId}/assets
     POST /api/customer/{customerId}/devices
```
①-③ 租户管理员在 App"农户管理"页完成；④ 分配田块/设备（App 提供勾选界面，或第二版先做"全部分配"）。

### 3.3 首次登录强制改密（微服务端标记 + App 流程）

**TB 无"强制改密"标志**（User 实体无该字段），用微服务端实现：

- `task_service` 库新增表 `user_pwd_flag`：`id`、`email`(唯一)、`must_change_password`(bool)、`created_at`
- 写入时机：注册成功（3.1⑥）、农户创建成功、预置演示账号初始化 → 均置 `true`
- App 登录流程：
```
① 登录 TB（POST /api/auth/login）成功
② 调微服务端 GET /api/auth/must-change-password?email=xxx
③ 若 true → 跳"修改密码"页（输入旧密码+新密码）
④ App 直连 TB：POST /api/auth/changePassword {currentPassword, newPassword}
    （TB 公开 API，需 Bearer token，旧密码校验+新密码策略由 TB 处理）
⑤ 成功后调微服务端 POST /api/auth/pwd-changed {email} → 标记 false
⑥ 进入主界面
```
- 改密成功前，主界面不可进入（拦截跳转）

### 3.4 预置演示账号（模拟器数据专用）

| 项 | 值 |
|----|-----|
| 账号 | `guanliyuan@thingsboard.org` |
| 初始密码 | `789456`（首次登录强制改密） |
| 角色 | 租户管理员（TENANT_ADMIN） |
| 数据 | 9 田块 + 27 台设备（9 田块 × 3 台，与第一版一致） |

**新脚本 `simulators/provision_demo_tenant.py`**（幂等，可重跑）：
```
① SysAdmin token → POST /api/tenant {title:"演示租户"}
② POST /api/user → guanliyuan@thingsboard.org / 789456（activationLinkInfo+activate 设密）
③ 在租户内重建田块+设备（复用 tb_setup.py 的 create 逻辑：Asset FIELD、DeviceProfile、
   Relation Contains、取 credentials 落盘）
④ 生成 simulators/device_inventory.json（指向新租户设备凭证）
⑤ 标记 guanliyuan 需强制改密
```
**说明（写入实现）**：App 里"新增设备/田块"**不会**产生模拟器——模拟器设备必须由脚本预置，这是预期行为。演示/验收流程：脚本跑完 → 模拟器按新 inventory 连接 → 用 guanliyuan 登录看到全部数据。

---

## 4. 田块/设备管理（App 直连 TB，仅 TENANT_ADMIN）

### 4.1 田块

| 操作 | API | 备注 |
|------|-----|------|
| 新增田块 | `POST /api/asset` | body `{name, type:"FIELD", assetProfileId}`；assetProfileId 从 `GET /api/assetProfileInfos?textSearch=FIELD` 取 |
| 田块列表 | `GET /api/tenant/assetInfos` | 第一版已有 |
| 田块下设备 | `GET /api/relations?fromType=ASSET&fromId={id}&relationType=Contains` | 第一版已有 |
| 删除田块 | `DELETE /api/asset/{id}` | **先解绑**：查该田块 relation 逐个 `DELETE /api/relation`（from 或 to=该田块），设备保留为"未挂载" |

### 4.2 设备

| 操作 | API | 备注 |
|------|-----|------|
| 新增设备 | `POST /api/device` | body `{name, deviceProfileId}`；deviceProfileId 从 `GET /api/deviceProfileInfos?textSearch=VALVE` 或 `...TEMPERATURE_HUMIDITY` 取（类型二选一，创建后不可改） |
| 挂载田块 | `POST /api/relation` | body `{from:{entityType:ASSET,id}, to:{entityType:DEVICE,id}, type:"Contains"}` |
| 设备列表 | `GET /api/tenant/deviceInfos` | 第一版已有 |
| 设备凭证 | `GET /api/device/{id}/credentials` | 返回 `credentialsId` = accessToken，App 展示/复制 |
| 删除设备 | `DELETE /api/device/{id}` | **先取消任务**：调微服务端 `DELETE /api/tasks/device/{deviceId}`（新增，取消该设备 PENDING/RUNNING 任务）；再解绑 relation、删设备 |

---

## 5. 任务表改造（微服务端）

`task-service/src/main/java/com/irrigation/task/entity/Task.java` 增加字段：

```java
@Column(name = "tenant_id", nullable = false)
private Long tenantId;
```

- `ddl-auto: update` 会自动加列（第一版现有表结构保留）
- **tenantId 来源**：App 登录 TB 的 JWT payload 含 `tenantId`，App 解析后随任务请求提交（`POST /api/tasks` body 增加 `tenantId`）；查询/取消任务带 `tenantId` 过滤
- `TaskController` 改造：创建校验 tenantId 非空；`GET /api/tasks`、`DELETE /api/tasks/{id}` 按 tenantId 过滤
- 新增 `DELETE /api/tasks/device/{deviceId}`（删除设备时取消其任务，见 4.2）

---

## 6. 模拟器

- 模拟器协议与第一版完全一致（HOST=192.168.50.138:1883 已修复 mosquitto 抢占问题），**连接逻辑零改动**
- 新增 `provision_demo_tenant.py` 后，`device_inventory.json` 指向演示租户的设备凭证；模拟器启动即向演示租户上报数据
- 旧租户（15079983758@163.com）下的第一版设备保留不动，可继续用旧账号查看（无强制改密，向后兼容）

---

## 7. UI 规格（阀门卡片升级）

目标：**大气、好看、数据直接可见**。Compose 卡片设计（`kotlin-demo` 田块详情页 DeviceCard）：

```
┌──────────────────────────────────┐
│ 🌊 田地1-灌溉阀门A      ● 工作中   │  ← 顶部：设备名 + 状态徽标（工作中=蓝/绿，未工作=灰）
│                                  │
│        ╭─────────╮               │  ← 中间：大号渐变开关（圆形，直径 ≥72dp）
│        │  开/关   │               │     工作中=明亮渐变，未工作=灰；点击切换，动画过渡
│        ╰─────────╯               │
│                                  │
│  瞬时流量 12.5 L/min  水压 0.35MPa│  ← 底部：数据行（工作状态实时刷新，未工作显示 "--"）
│  累计用水 3.24 m³     电量 85%    │
└──────────────────────────────────┘
```

- 卡片：圆角 16dp、浅阴影、留白充足；状态徽标带圆点+文字
- 数据字段：`instantFlow`(L/min)、`waterPressure`(MPa)、`totalWaterUsage`(m³)、`batteryLevel`(%)——来自现有遥测 key，**无需新增上报字段**
- 温度湿度计卡片同步微调：温度/湿度大数字展示（第一版已有基础，统一卡片风格）

---

## 8. 具体实现步骤（按顺序，每步可验证）

| 步骤 | 内容 | 涉及文件 | 验证方式 |
|------|------|---------|---------|
| **1. TB 安全与预置** | 改 TB admin 默认密码；写 `provision_demo_tenant.py` 建演示租户+guanliyuan+9 田块 27 设备；重启模拟器 | `simulators/provision_demo_tenant.py`、`device_inventory.json` | guanliyuan 登录 TB Web 看到设备上报 |
| **2. 微服务端注册** | `ThingsBoardProperties` 加 sysadmin 配置；`ThingsBoardClient` 加 `registerTenant()`（5 步）；新增 `AuthController`(`/api/auth/register`) | `task-service/.../config/ThingsBoardProperties.java`、`service/ThingsBoardClient.java`、新增 `controller/AuthController.java`、`application.yml` | curl 注册 → TB 出现新租户 |
| **3. 微服务端强制改密** | 新增表 `user_pwd_flag` + Repository + `AuthController` 加 `GET /api/auth/must-change-password`、`POST /api/auth/pwd-changed` | 新增 `entity/UserPwdFlag.java`、`repository/UserPwdFlagRepository.java`、改 `AuthController` | 注册后查标记=true；改密后=false |
| **4. 微服务端任务隔离** | `Task.java` 加 `tenantId`；Controller 按租户过滤；新增 `DELETE /api/tasks/device/{deviceId}` | `Task.java`、`TaskController.java`、`TaskRepository.java` | 两租户任务互不可见 |
| **5. APP 基础** | JWT 持久化（SharedPreferences/DataStore）；解析 JWT 取 tenantId/customerId/authority；登录页加注册入口；首次登录强制改密流程 | `data/api/ApiClient.kt`、`AuthInterceptor.kt`、`ui/screens/Screen1.kt` 等 | 注册→登录→强制改密→进主界面 |
| **6. APP 管理页** | 田块新增/删除；设备新增/删除/挂载田块/凭证展示（token 复制）；农户管理（建客户+用户+分配） | 新增 `ui/screens/FarmManageScreen.kt`、`DeviceManageScreen.kt`、`UserManageScreen.kt`；`ThingsBoardApi.kt` 加接口 | 建田块→建设备→挂载→模拟器数据出现 |
| **7. 阀门 UI 升级** | 按 §7 规格重做 DeviceCard（大开关+数据行） | `ui/screens/` 田块详情卡片 | 视觉验收 |
| **8. 联调验收** | 全流程：注册租户A → 建田块设备 → 建农户 → 农户登录操作；guanliyuan 演示账号全量数据 | — | 按《第二版用户需求文档》逐条过 |

---

## 9. 相对第一版变动清单（实现层面）

**新增**
1. 微服务端 `POST /api/auth/register`（租户注册，SysAdmin 建租户+管理员）
2. 微服务端强制改密标记（表 `user_pwd_flag` + 2 个接口）
3. 微服务端 `DELETE /api/tasks/device/{deviceId}`（删设备联动取消任务）
4. APP 注册入口、首次登录强制改密页、农户管理页、田块管理页、设备管理页（含凭证展示）
5. 模拟器 `provision_demo_tenant.py`（演示租户 + guanliyuan 账号）

**修改**
6. `Task` 表加 `tenantId`，任务按租户隔离（原全局可见）
7. APP 查询接口按角色切换（TENANT_ADMIN 用 tenant 接口 / CUSTOMER_USER 用 customer 接口）
8. JWT 内存存储 → 持久化
9. 阀门卡片 UI 重做（大开关 + 数据行）
10. `application.yml` 加 SysAdmin 凭证配置；TB 默认 admin 密码修改

**保持不变**
11. 模拟器 MQTT 协议、上报频率、数据字段（`instantFlow`/`waterPressure`/`totalWaterUsage`/`batteryLevel`/`temperature`/`humidity`）
12. 定时任务四态流转与冲突检测算法
13. 田块=Asset、设备挂载=Relation(Contains) 的数据模型
14. 仪表板「智能灌溉总览」与 TB 侧预置脚本

---

## 10. 已知限制（实事求是）

- **App 新增设备不产生模拟器**：模拟器设备必须脚本预置（第 3.4/6 节已给出演示方案）；真设备接入不受影响（见《第二版设计方案.md》§3.5）
- **TB CE 无自助注册**：注册必须经微服务端（SysAdmin 代建），无邮件激活（激活链接 API 无邮件也可用）
- **强制改密靠微服务端标记**：TB 无原生标志位；App 端逻辑拦截进入主界面
- **租户数量与承载**：单机 TB 官方基准 10 万设备/111msg/s，本系统负载差 60 倍余量；第二版不触及扩展
