# 智能灌溉系统 第二版内部需求文档

> 版本：v2.1（2026-08-06，按《新建 DOC 文档.doc》需求更新）
> 读者：开发人员。本文档从**开发视角**描述第二版要实现什么、接口怎么定、改哪些文件。
> 配套：《第二版用户需求文档.md》（用户视角）、《第二版设计方案.md》（技术依据）
> 需求来源：《物联网灌溉系统第二代第一版/新建 DOC 文档.doc》7 条需求 + 前版文档，所有接口已对照部署的 ThingsBoard swagger（8.138.135.149:8080）核实。

---

## 1. 总体架构（第二版）

```
App（Android）
 ├─ 登录/日常操作 ──直连──▶ ThingsBoard REST（8080）   ← 保持第一版架构
 │      （田块/设备/遥测/RPC/凭证/改密 全部直连 TB）
 ├─ 注册/强制改密标记 ──▶ 微服务端（:9300）            ← 第二版新增（TB CE 4.x 无自助注册）
 └─ 任务管理 ──▶ 微服务端（:9300）
模拟器/真设备 ──MQTT──▶ ThingsBoard（1883）
微服务端 ──持 SysAdmin token──▶ ThingsBoard（建租户/用户）
```

**原则**：能直连 TB 的绝不经过微服务端；微服务端只负责 TB 做不到的——注册建租户（需 SysAdmin）、强制改密标记、定时任务（加租户隔离）。

---

## 2. 角色与权限（已确认：使用者可操作但不可管理）

| App 角色 | TB Authority | 权限范围 |
|---------|-------------|---------|
| 租户管理员 | `TENANT_ADMIN` | 本租户全部：田块/设备增删、挂载、使用者管理、任务 |
| 使用者（客户用户） | `CUSTOMER_USER` | **可操作**：看数据、开关阀（单控/批量）、创建/取消定时任务 |
| 使用者 | `CUSTOMER_USER` | **不可管理**：新增/删除田块设备、挂载、新增使用者——全部 ❌ |
| （内部）SysAdmin | `SYS_ADMIN` | 平台级，凭证只放微服务端配置 |

**实现要点**：
- TB 原生保证：田块/设备**分配**给 Customer 后，Customer 用户可查看/控制（RPC），但 `POST /api/device`、`POST /api/asset`、`POST /api/customer` 等管理接口对 CUSTOMER_USER 返回 403，无需自写权限判断
- 挂载设备（改归属）= 管理操作，**仅租户管理员可用**
- App 查询接口按角色切换：管理员用 `/api/tenant/deviceInfos`、`/api/tenant/assetInfos`；使用者用 `/api/customer/{customerId}/deviceInfos`、`/api/customer/{customerId}/assetInfos`

---

## 3. 注册与账号（按 doc 需求 2 更新）

### 3.1 租户注册（微服务端新增接口）

```
POST /api/auth/register            （微服务端 :9300）
body:  { "email": "...", "phone": "..." }   ← doc：注册不设密码，用默认密码
响应:  { "success": true, "message": "注册成功" }
```

微服务端内部（`ThingsBoardClient.registerTenant()`，RestTemplate 调 TB）：
```
① SysAdmin 登录（配置中的 admin 账号密码）
② POST /api/tenant {title: email}                    → tenantId
③ POST /api/user?sendActivationMail=false
     {email, authority:"TENANT_ADMIN", tenantId}       → userId（新用户默认未激活）
④ GET  /api/user/{userId}/activationLinkInfo           → activateToken
⑤ POST /api/noauth/activate {activateToken, password:"默认密码 123456"}
⑥ 微服务端记录 must_change_password=true
```

**默认密码方案（已确认）**：默认密码统一 `123456`；
**注册成功自动登录（doc 需求 2）**：注册接口成功后，App 直接调 TB `POST /api/auth/login`（email + 123456）→ 拿到 token 自动进入主界面；
**首次改密不需要当前密码（doc 需求 2）**：TB 的 `POST /api/auth/changePassword` 必须校验当前密码 → **App 代填**：改密页的"当前密码"字段自动填 `123456`（用户不可见/只读），用户只需输入新密码+确认 → 调 changePassword。体验上"不需要当前密码"，且不绕过 TB 安全校验。

**安全注意**：默认密码 `123456` 是弱口令，注册到改密窗口期间有被撞库风险（可接受，改密流程立即触发）；SysAdmin 凭证只放服务端配置，且**必须先改 TB admin 默认密码**。

### 3.2 常见问题页（doc 需求 2）

- 登录页新增"常见问题"入口（静态文案页）
- 文案（按 doc）："如果用户没使用过，可以注册农田，并默认成为农田管理员，不能直接成为农田使用者；注册成功后自动登录，默认密码首次登录需修改。客户端无法直接新增 ThingsBoard 租户管理员，须经微服务端；租户管理员可直接在 App 内新增租户使用者（客户）。"

### 3.3 使用者账号（doc 需求 5，管理员在"我的"里新增）

```
① POST /api/customer {title: 使用者名称}                    → customerId
② POST /api/user?sendActivationMail=false
     {email, authority:"CUSTOMER_USER", customerId}          → userId
③ GET  /api/user/{userId}/activationLinkInfo → POST /api/noauth/activate（默认密码 123456）
④ 分配可见范围（勾选田块/设备）：
     POST /api/customer/{customerId}/asset/{assetId}
     POST /api/customer/{customerId}/device/{deviceId}
```
- 入口：底部「我的」→「使用者管理」（仅租户管理员可见）
- 支持删除使用者：`DELETE /api/customer/{customerId}`（或仅 `DELETE /api/user/{userId}`）

---

## 4. 田块管理（doc 需求 3）

### 4.1 田块列表（第一版已有）
田块总览网格卡片，每卡片显示名称 + 设备数；点击进入详情。

### 4.2 新增田块
- 田块页顶部加「新增田块」按钮 → 弹窗填名称 → `POST /api/asset {name, type:"FIELD", assetProfileId}`（assetProfileId 从 `GET /api/assetProfileInfos?textSearch=FIELD` 取）→ 保存/取消

### 4.3 删除田块（doc：点卡片选择模式）
- 田块页顶部加「删除田块」按钮 → 进入选择模式（点击田块卡片选中，可多选）→ 「删除」或「取消」
- 二次确认弹窗：「将删除选中的 N 块田块，其下设备将变为自由设备（未挂载），是否继续？」
- 实现：`DELETE /api/asset/{id}`；删除前不删设备，仅解绑关系（设备保留为自由设备，可重新挂载）

---

## 5. 设备管理（doc 需求 4：自由设备 + 两种挂载）

### 5.1 新增设备（新增后为"自由设备"，不选田块）

- 「所有设备信息」页顶部加「新增设备」按钮
- 表单：**设备名称** + **类型单选**（仅：电动阀 VALVE / 温度湿度计 TEMPERATURE_HUMIDITY）——**无田块选择**（新增后是自由设备）
- 实现：
```
① GET  /api/deviceProfileInfos?pageSize=100&textSearch=VALVE（或 TEMPERATURE_HUMIDITY）→ deviceProfileId
② POST /api/device {"name":..., "deviceProfileId":{"entityType":"DEVICE_PROFILE","id":...}} → deviceId
③ GET  /api/device/{deviceId}/credentials → accessToken
④ 弹窗展示 token（复制/二维码），提示"配置到设备后数据自动出现"
```
- 可选优化：`POST /api/device-with-credentials` 一步创建（带自定义凭证），与 ②③ 等价

### 5.2 自由设备挂载（doc：两种方式）

**方式一：田块详情页挂载**
- 田块详情页加「挂载」按钮 → 弹窗列出**自由设备**（未挂载设备）→ 勾选 → 确定
- 自由设备列表获取：`GET /api/tenant/deviceInfos`（全部设备）− 已挂载设备（遍历田块 relations）＝ 自由设备（TB 无原生"未挂载"查询，此方案最简；设备量大时优化为一次性查全部 relations）

**方式二：设备页选田块挂载**
- 「所有设备信息」页每台自由设备提供「挂载」操作 → 弹窗选田块 → 确定

**两种方式最终都调用**：
```
POST /api/relation {"from":{"entityType":"ASSET","id":"<田块id>"},"type":"Contains","to":{"entityType":"DEVICE","id":"<设备id>"}}
```

### 5.3 删除设备（已确认新增）
- 设备信息页（含自由设备）提供删除操作 → 二次确认「将取消该设备所有未完成任务且不可恢复」
- 实现：先调微服务端 `DELETE /api/tasks/device/{deviceId}` 取消任务 → 再 `DELETE /api/device/{id}`（TB 删除设备自动清理其 relation）

---

## 6. 任务模块（doc 需求 6：红点已读）

- 第一版：主页面顶栏任务图标带 Badge（显示任务数）
- 第二版：**访问任务管理页后 Badge 消失**（会话内已读标记：App 本地状态 `hasVisitedTasks`，进入任务页置 true，退出登录重置）
- 任务四态、冲突检测、租户隔离（tenantId）沿用前版规划

---

## 7. UI / Logo（doc 需求 7）

- APP 整体"大气"：统一 Material3 紫色主题（沿用第一版）、卡片圆角/留白/阴影统一
- 图标 Logo 好看：替换默认图标为自定义（田块/设备/阀门/温度计图标），应用图标（ic_launcher）用定制 Logo
- 具体样式以《第二版UI原型.html》为准（基于第一版改造版）

---

## 8. 微服务端任务表改造

`task_schedule` 加 `tenant_id`（可空，第一版数据为 null）：任务创建/查询/取消按租户过滤；新增 `DELETE /api/tasks/device/{deviceId}`。

---

## 9. 模拟器

- 协议不变（MQTT，HOST=192.168.50.138:1883）
- `provision_demo_tenant.py`：SysAdmin 建演示租户 + 15079983758@163.com/147258（首登强制改密）+ 9 田块 27 设备（复用 tb_setup 建模逻辑）
- **APP 新增设备不自动产生模拟器进程**（预期行为）：新 token 可手动 `py valve_simulator.py <token> 192.168.50.138 1883` 拉起；演示用预置账号

---

## 10. 具体实现步骤（按顺序，每步可验证）

| 步骤 | 内容 | 验证 |
|------|------|------|
| 1. TB 安全 | 改 TB admin 默认密码；写 provision_demo_tenant.py | 15079983758 登录看到数据 |
| 2. 微服务端注册 | `POST /api/auth/register`（SysAdmin 建租户+管理员+默认密码 123456 激活）；`user_pwd_flag` 表 | curl 注册→TB 出现新租户 |
| 3. 微服务端改密标记 | `GET /api/auth/must-change-password`、`POST /api/auth/pwd-changed` | 注册后=true，改密后=false |
| 4. 微服务端任务隔离 | Task 加 tenantId（已加字段）；按租户过滤；`DELETE /api/tasks/device/{deviceId}` | 两租户任务互不可见 |
| 5. APP 账号流 | 登录页：注册入口+常见问题；注册→自动登录→改密页（App 代填默认密码）；JWT 持久化 | 注册→自动登录→改密→进主界面 |
| 6. APP 田块管理 | 新增田块/删除田块（点卡片选择模式）；删除后设备变自由设备 | 建/删田块，设备变自由 |
| 7. APP 设备管理 | 新增设备（自由设备+token 展示）；两种挂载；删除设备（联动取消任务） | 建设备→挂载→模拟器数据出现 |
| 8. APP 使用者管理 | 「我的」→使用者管理：新增/删除使用者、分配田块设备 | 使用者登录→可操作不可管理 |
| 9. 任务红点 | 访问任务页后 Badge 消失 | 进任务页→红点消失 |
| 10. UI/Logo | 按原型统一样式；换应用图标 | 视觉验收 |
| 11. 联调 | 全流程端到端 | 按需求文档逐条过 |

---

## 11. 接口清单（对照部署 TB swagger 8.138.135.149:8080 核实，全部存在）

### 11.1 ThingsBoard 原生接口（App 直连，:8080）

| 方法 | 路径 | 用途 |
|------|------|------|
| POST | /api/auth/login | 登录（注册后自动登录也用） |
| POST | /api/auth/logout | 退出 |
| POST | /api/auth/changePassword | 修改密码（需当前密码，App 代填） |
| POST | /api/auth/token | 刷新 JWT |
| POST | /api/tenant | 【SysAdmin】创建租户（注册用） |
| POST | /api/user | 创建用户（SysAdmin 建管理员 / 管理员建使用者） |
| GET | /api/user/{userId}/activationLinkInfo | 获取激活 token（注册/建使用者设默认密码用） |
| POST | /api/noauth/activate | 激活用户并设置密码（默认密码 123456） |
| DELETE | /api/user/{userId} | 删除使用者账号 |
| GET | /api/tenant/deviceInfos | 管理员：设备列表（含自由设备） |
| GET | /api/tenant/assetInfos | 管理员：田块列表 |
| POST | /api/device | 创建设备 |
| DELETE | /api/device/{deviceId} | 删除设备（自动清理 relation） |
| GET | /api/device/{deviceId}/credentials | 获取 accessToken（设备凭证展示） |
| POST | /api/device-with-credentials | 【可选】带凭证一步创建设备 |
| GET | /api/deviceProfileInfos | 查设备类型 profile（VALVE / TEMPERATURE_HUMIDITY） |
| POST | /api/asset | 创建田块 |
| DELETE | /api/asset/{deviceId} | 删除田块 |
| GET | /api/assetProfileInfos | 查田块 profile（FIELD） |
| POST | /api/relation | 挂载/解绑设备与田块（Contains） |
| DELETE | /api/relation | 删除关系（解绑） |
| GET | /api/relations?fromType=ASSET&fromId=... | 查田块下设备（第一版已有） |
| GET | /api/relations/info | 查关系详情（自由设备计算用） |
| POST | /api/customer | 创建使用者（客户） |
| DELETE | /api/customer/{customerId} | 删除使用者 |
| GET | /api/customers | 使用者列表 |
| GET | /api/customer/{customerId}/users | 使用者下账号 |
| POST | /api/customer/{customerId}/device/{deviceId} | 分配设备给使用者 |
| POST | /api/customer/{customerId}/asset/{assetId} | 分配田块给使用者 |
| GET | /api/customer/{customerId}/deviceInfos | 使用者：设备列表 |
| GET | /api/customer/{customerId}/assetInfos | 使用者：田块列表 |
| GET | /api/plugins/telemetry/DEVICE/{deviceId}/values/timeseries | 最新/历史遥测（第一版已有） |
| POST | /api/rpc/oneway/{deviceId} | RPC 开关阀（第一版已有） |

### 11.2 微服务端自定义接口（:9300）

| 方法 | 路径 | 用途 |
|------|------|------|
| POST | /api/auth/register | 租户注册（SysAdmin 建租户+管理员+默认密码激活） |
| GET | /api/auth/must-change-password?email= | 查询是否需强制改密 |
| POST | /api/auth/pwd-changed | 标记已改密 |
| POST | /api/tasks | 创建任务（单/批量，带 tenantId） |
| GET | /api/tasks?tenantId= | 查询任务（按租户过滤） |
| DELETE | /api/tasks/{id} | 取消任务（软删除） |
| DELETE | /api/tasks/device/{deviceId} | 删除设备时取消其任务 |

---

## 12. 相对第一版的改动总结

**新增（doc 需求对应）**
1. 底部导航加「我的」Tab（doc 1）：角色标识、修改密码、退出、使用者管理入口
2. 登录页注册 + 常见问题（doc 2）：注册=默认密码 123456+自动登录+App 代填旧密码改密
3. 田块：新增田块、删除田块（点卡片选择模式，设备变自由设备）（doc 3）
4. 设备：新增设备（自由设备+token 展示）、两种挂载方式、删除设备（doc 4）
5. 使用者管理：管理员在「我的」新增/删除使用者、分配田块设备（doc 5）
6. 任务红点访问后消失（doc 6）
7. UI/Logo 大气美观（doc 7）
8. 首次登录强制改密（默认密码场景）
9. 任务按租户隔离（tenantId 字段已加）

**修改**
10. 新增设备不再选田块（改为自由设备 + 事后挂载，与 TB 模型一致）
11. App 查询接口按角色切换（TENANT_ADMIN / CUSTOMER_USER）
12. JWT 内存存储 → 持久化
13. 微服务端：注册接口、改密标记表、任务租户隔离、`DELETE /api/tasks/device/{deviceId}`

**保持不变**
14. 模拟器 MQTT 协议、上报频率、数据字段；定时任务四态与冲突检测；田块=Asset、挂载=Relation(Contains)；仪表板

---

## 13. 已知限制（实事求是）

- **TB CE 4.x 无自助注册**：注册必须经微服务端（SysAdmin 代建）；"注册后自动登录"由 App 调 login 实现
- **改密必须当前密码**：TB 无免旧密码改密；用 App 代填默认密码实现"不需要当前密码"体验
- **使用者不能管理**：Customer 用户调管理接口 403（这是特性不是 bug）
- **自由设备列表无原生查询**：需"全部设备 − 已挂载"计算
- **APP 新增设备不自动产生模拟器进程**：模拟器需脚本预置或手动拉起
- 承载能力：单机 TB 官方基准 10 万设备/111msg/s，本系统负载差 60 倍余量
