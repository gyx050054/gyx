# 本次改动说明 & 与需求文档差异对照

> 日期：2026-08-25
> 范围：本会话从「需求审核」到「第三代第一版功能落地」的全部工作
> 代码基址：`D:\灌溉系统\gyx`（git 仓库 github.com/gyx050054/gyx，当前 HEAD `ca3dc7f`）

---

## 1. 本次会话做了什么（一句话）

在现有「第一代第三版」灌溉系统（田间设备→ThingsBoard→微服务端 task-service→Android App）基础上，完成了：

1. **审核并修订两份主需求文档**（用户需求文档、内部需求文档）。
2. **实现「第三代第一版」设计方案的五个功能**：土壤墒情、自定义告警、天气/短信桩、每天自动浇水、田块地图。
3. **把已实现功能补写进需求文档**、**把设计/需求文档与自研实现对齐**。

---

## 2. 代码改动明细（本次会话实际动手的部分）

### 2.1 土壤墒情（功能二，对齐设计文档 §3）
- **统一报文字段为「盐分+pH」**：模拟器 `soil_simulator.py` 上报 `soilSalinity / soilPh`；
  App 端 `data/model/Device.kt`、`data/api/AppConfig.kt`、`DeviceDialogs.kt`、`DeviceListComponents.kt`、`FieldDetailScreen.kt` 从旧的 `soilMoisture / soilTemperature` 全部改为盐分/pH，旧键名清零。
- **墒情历史曲线**：`HistoryScreen.kt` 通用化（新增 `deviceType` 参数），SOIL_DEVICE 显示盐分/pH 曲线；NavGraph/MainScreen/FieldDetail/DeviceList 全链路传设备类型。
- 既有改动（非本次）：`provision_demo_tenant.py` 预置 5 台墒情机、`start_all.py` 含 soil_simulator、`config.py` 增 `SOIL_INTERVAL`。

### 2.2 自定义告警（功能四，自研引擎）
- **微服务端 task-service**（新增）：
  - `service/AlarmService.java` —— 规则 CRUD + 每 30 秒扫描遥测判阈值 + 命中建/刷新告警 + 恢复置 RESOLVED + 确认 + 未确认计数。
  - `service/AlarmScanScheduler.java` —— 每 30 秒触发，间隔可配 `alarm.scan-interval-ms`。
  - `controller/AlarmController.java` —— `/api/alarms/**`：规则增删改查/启用停用、告警列表、`unread-count`、`ack`。
  - `service/ThingsBoardClient.java` —— 新增 `listDevicesByType` / `latestTelemetry`。
  - 复用既有（本次接线）：`entity/AlarmRule`、`entity/AlarmRecord`、`AlarmRuleRepository`、`AlarmRecordRepository`。
- **App（kotlin-demo）**（新增）：`data/api/AlarmRepository.kt`、`viewmodel/AlarmViewModel.kt`、`ui/screens/AlarmScreen.kt`（红点/列表/确认/级别色标）、`ui/screens/AlarmRuleScreen.kt`（规则管理）、`ui/screens/MainScreen.kt`（顶栏铃铛+红点，15 秒轮询）、`NavGraph`/`TaskServiceApi`/`ThingBoardDtos`（告警接口与 DTO）。

### 2.3 天气 + 短信桩（功能三）
- **微服务端**（新增）：`service/WeatherService.java`（Open-Meteo 网关 + 10 分钟缓存 + 降级）、`controller/WeatherController.java`（`GET /api/weather`）、`service/MessageSender.java` 接口 + `service/LogSmsSender.java`（桩，只打日志不真发）。
- **App**（新增）：`ui/screens/WeatherScreen.kt` 天气板块、`MainScreen.kt` 加第 4 个「天气」tab（田块/设备/天气/我的）、`TaskServiceApi`/`TaskRepository`/`ThingBoardDtos` 加天气接口与 `WeatherDto`。

### 2.4 每天自动浇水（功能一）
- **微服务端**：
  - `entity/Task.java` —— 加 `repeatMode(ONCE/DAILY)`、`dailyHour`、`durationMinutes`字段。
  - `entity/TaskRun.java`、`repository/TaskRunRepository.java`（新增）—— `task_runs` 流水表，按 `taskId+runDate` 去重。
  - `service/DailyTaskService.java`（新增）—— 每天到点开浇/到时关浇/按日去重/降雨概率≥80% 自动跳过（SKIPPED_WEATHER）。
  - `service/TaskService.java` —— 支持 DAILY 创建与冲突（同设备唯一、与 daily 窗口重叠判定）。
  - `service/TaskScanScheduler.java` —— 接入 `processDaily`。
  - `controller/TaskController.java` —— 支持 DAILY 参数 + `GET /api/tasks/{id}/runs`（流水查询）。
- **App**：`ui/components/DeviceDialogs.kt` 的 `TimeRangeDialog` 加「每天自动浇水」开关（MainScreen/FieldDetailScreen 两处调用点同步）、`data/model/Task.kt` 加 repeatMode 字段、`TaskManagementScreen.kt` 任务列表显示「🔁 每天 HH:00 浇 X 分钟」、`TaskServiceApi`/`TaskRepository`/`TaskViewModel`/`ThingBoardDtos` 支持每天参数。

### 2.5 田块地图（功能五）
- **App**（新增）：`assets/map.html`（Leaflet 1.9.4 + OpenStreetMap，免费无 key）、`ui/components/FieldMapView.kt`（WebView + 坐标注入 + 设备点击回调到开关弹窗 + 无坐标设备按田块中心偏移展示）、`data/model/Field.kt`/`Device.kt` 加 `lat/lon` 字段、`FieldDetailScreen.kt` 顶栏「地图/列表」切换。

### 2.6 需求文档修订（第一代第三版）
- `物联网灌溉系统_用户需求文档.md`：补「3.2 账号与用户管理」（注册/首登强制改密/手动改密/成员管理/FAQ）、修错别字、补「10 秒刷新 vs 设备上报频率」关系说明、章节编号顺延。
- `智能灌溉田块管理APP内部需求.md`：补 `3.4.1` 工作状态上报缺的 `valveState` 字段、补角色表、新增「第 7 章 账号与用户管理体系」、补 `deviceInfos` 分页/`pauseValve` 语义/演示加速说明、删重复条目。

### 2.7 第三代文档与实现对齐
- `第三代第一版/第三代第一版·设计方案.md` → 升 v2.1，决策表/§5/§9 的「用 ThingsBoard 告警」改为「微服务端自研引擎」，待办清单改为「实现情况确认」。
- `第三代第一版/第三代第一版·用户需求文档.md` → 升 v2.1，§3.4 告警实现方式、天气坐标、地图入口等与实现对齐。

---

## 3. 与需求文档的差别在哪里

> 需求文档指：`第三代第一版/第三代第一版·用户需求文档.md` + `第三代第一版/第三代第一版·设计方案.md`（本会话据此实现）。

### 3.1 实现做了、需求原本没写（现已补进文档）
| 项 | 说明 |
|---|---|
| 土壤墒情键名 | 需求 §3.2 原来用「盐分/pH」；但代码（既有的 App）用的是 `soilMoisture/soilTemperature`。本次**以需求为准**把 App 全部改为 `soilSalinity/soilPh`。 |
| 天气坐标 | 需求 §3.3 写「自动获取定位」；实现先用了**默认坐标（南昌 28.6/115.9）**占位，文档已标注。 |
| 地图入口 | 需求 §3.5 写「田块总览提供标准/详细切换」；实现为 **田块详情页顶栏「地图」按钮切换**（非总览页），文档已标注。 |

### 3.2 实现与需求文档不一致的地方
| 编号 | 需求文档怎么写 | 实现做了什么 | 处理 |
|---|---|---|---|
| 1 | 设计方案 v2.0：告警用 **ThingsBoard Alarm Rules**（§5） | 经你拍板改为**微服务端自研告警引擎**（AlarmService 扫遥测） | 已在设计/用户需求文档注明 v2.1 变更，并保留 TB 方案作「不再采用的备选」 |
| 2 | 设计 §3.1：墒情机上报 `soilSalinity/soilPh` | App 既有代码在等 `soilMoisture/soilTemperature`，两端对不上（断链） | 已把 App 改为 `soilSalinity/soilPh`，断链修复 |
| 3 | 设计 §2 每天任务：到点开浇 | 实现中 DAILY 任务用 **dailyHour 驱动** + 落库写当天窗口作 initial 占位（设计 §9 原列为「实施时定」） | 已在 v2.1 文档写明取舍 |
| 4 | 设计 §5.5 示例含「土壤水分低于 X」 | 因墒情机用盐分/pH，示例改为「土壤盐分高于 X」 | 已在需求文档 §3.4 对齐 |

### 3.3 需求写了、但本次**未实现/未做完整**
| 项 | 现状 | 说明 |
|---|---|---|
| 天气板块「按田块定位」 | 用具默认坐标 | 未来可扩展为按田块坐标/定位（本轮未做） |
| 田块地图「田块总览页标准/详细切换」 | 只做了田块**详情页**地图 | 总览页地图未做 |
| 运行态真实联调 | 服务端全链路已联调验证（见 §4.1）；App 真机瓦片显示未确认 | 端到端联调于 2026-08-26 完成，但地图瓦片真机显示因模拟器 adb 交互不稳未逐项确认 |
| 完整 `mvn package` 打包 | 受离线缓存缺 surefire 插件限制未成 | 非代码问题；`mvn compile` 已验证 |

---

## 4. 边界说明（不虚标）

- **编译验证**：task-service `mvn -o compile` 通过；App `gradlew compileDebugKotlin` 多次 BUILD SUCCESSFUL。
- **App↔微服务端接口闭环**：App 端 18 个微服务接口与 Alarm/Auth/Task/Weather 四 Controller 路由逐一匹配。
- **运行期依赖**：MySQL :3306、ThingsBoard :8080、task-service :9300、27 台设备模拟器 :1883 本次已全程拉起并实测（见 §4.1）。

### 4.1 端到端联调验证记录（2026-08-26，实际联调）
> 前提：Docker(MySQL/ThingsBoard) 已运行；task-service 由 IDEA 以 target/classes 最新代码启动（PID 含 idea_rt.jar）；27 台模拟器含 5 台墒情机已连 MQTT。

| 链路 | 实测输入 | 实测输出 | 结论 |
|---|---|---|---|
| 天气 | GET `/api/weather`（默认坐标 南昌） | 200 `{"weatherDesc":"多云","temperature":24.0,"precipProb1h":0}` | ✅ |
| 告警触发 | POST `/api/alarms/rules` 建规则 `SOIL_MOISTURE/soilSalinity/gt/300`（当前盐分 415.5） | 扫描后生成 5 条 ACTIVE 告警（5 台墒情机）；`unread-count`=5 | ✅ |
| 告警确认 | POST `/api/alarms/{id}/ack` | 单条 ACTIVE→ACKNOWLEDGED，`unread-count` 5→4 | ✅ |
| 告警自动恢复 | 同规则升阈值 `gt/5000`（415.5>5000=false） | 44s 内 5 条记录全部 ACTIVE→**RESOLVED**，`unread-count`=0 | ✅ |
| 土壤墒情遥测 | TB 读墒情机最新遥测 | `soilSalinity=415.5 / soilPh=7.03`，5 台 SOIL_MOISTURE 设备均已上报 | ✅ |
| 每天自动浇水 | POST `/api/tasks` 建 DAILY 任务 `dailyHour=18/durationMinutes=5` | 任务 id=6 全生命周期真实跑通：`PENDING→RUNNING(18:00)→COMPLETED(18:05)`，开阀 RPC 生效（18:01 阀 `valveState=WORKING`、`instantFlow=11.6`、累计用水 5.87L），18:05 关阀生效（`valveState=IDLE`） | ✅ 开/关浇 RPC 链路真实验证；⚠ 但 `task_runs` 流水确实未写入（见下 bug） |
| 地图/告警红点/天气 App UI | Android 模拟器(Pixel_7_API35) `emulator-5554` 上 `com.demo.kotlindemo` 已安装并运行 | adb 交互不稳，真机逐项 UI 未全部确认 | ⚠ 待实机逐项确认 |

**联调中发现的真实缺陷（已修源码）：**
- **`AlarmController.updateRule` 部分更新 bug**：原用 `mapper.convertValue(body, AlarmRule.class)` 反序列化，对 JSON 缺失字段会填充实体默认初值（`deviceType` 默认 `ALL`、`operator` 默认 `lt`），导致「只更新 threshold」时会把 `deviceType/operator` 意外重置为默认值。已改为**逐字段读 body、仅设置显式出现字段**，`mvn -o compile` 通过。**注意：IDEA 当前运行的仍是旧 class，需在 IDEA 重新 Run 一次使修复生效后才可复测。**

**联调中发现的真实缺陷（未修，待处理）：**
- **DAILY 任务与 TaskScanScheduler 双重调度冲突 → task_runs 流水失效**：DAILY 任务被 `TaskScanScheduler.scanAndExecute()` 的 ①（`findByStatusAndStartTimeLessThanEqual(PENDING, now)` 会捞到 DAILY）当成普通到点任务，置为 `RUNNING` 并直接开阀、落 start/end_time；而 `DailyTaskService.processDaily()` 用 `findByRepeatModeAndStatus(DAILY, PENDING)` 只处理 **PENDING** 任务，因此任务一旦被 ① 置 RUNNING，`processDaily` 就匹配不到 → **`task_runs` 按日去重流水完全没写入（实测 task_id=6 记录数=0）**，且 DailyTaskService 的关浇/天气跳过/按日去重逻辑全部形同虚设（开浇其实由 ① 直发，关浇由 ② 兜底）。**影响**：开/关浇 RPC 仍能正确执行（本次已实测），但「每天自动浇水」的持久化流水、天气联动跳过、按日去重这三项实际未生效。**待修方向**：让 ①/② 跳过 `repeatMode=DAILY` 的任务（DAILY 完全交给 `processDaily` 处理），或由 `processDaily` 统一接管 DAILY 的开浇/关浇/流水。

---

## 5. 附：工作区改动清单（完整，76 项未提交）

> 以下为 `git status` 全部改动。其中部分（如 MainActivity、ThingsBoardApi、provision_demo_tenant、删除的 .bat、start-all.ps1 等）是**会话开始前已存在、未经本会话改动**的待提交内容，一并留在工作区。

- **本会话新增文件**（未跟踪 `??`）：告警/天气/每天任务/地图相关 —— AlarmController/AlarmService/AlarmScanScheduler/WeatherController/WeatherService/MessageSender/LogSmsSender/DailyTaskService/TaskRun(+Repo)/AlarmRule(+Repo)/AlarmRecord(+Repo)，App 端 AlarmRepository/AlarmViewModel/AlarmScreen/AlarmRuleScreen/WeatherScreen/FieldMapView/map.html/images。
- **本会话修改文件**（`M`）：App 端 Device/Field/Task/AppConfig/ThingBoardDtos/TaskServiceApi/TaskRepository/TaskViewModel/MainScreen/FieldDetailScreen/HistoryScreen/TaskManagementScreen/DeviceDialogs/NavGraph 等；task-service 端 Task/TaskService/TaskController/TaskScanScheduler/TaskRepository/ThingsBoardClient 等。
- **会话前既有改动**（`M`/`D`，本会话未触碰）：MainActivity、AuthRepository、ThingsBoardApi/Repository、MemberComponents/Dialogs、UserManagementScreen、Theme、TimeFormats、start-all.ps1、`一键启动.bat`/`启动Android模拟器.bat`（删除）、模拟器 provision/config/device_inventory/start_all、第二版相关文档（删除）、`第三代第一版/` 目录（未跟踪）等。

> 结论：本会话的**实质新增**集中在「第三代第一版功能一~五」的代码 + 三份需求/设计文档的对齐与修订；其余未提交改动为仓库既有待提交内容。
