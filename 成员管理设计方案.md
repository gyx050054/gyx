# 智能灌溉系统 成员管理设计方案（第三版功能）

> 版本：v1.0（2026-08-10）
> 背景：现状"1 个使用者 = 1 个客户 + 1 个客户用户"过于简化，无法表达"一个家庭（客户）多人共用一套设备"。
> 本文档确定实体关系模型、角色权限、功能流程、接口与实施步骤，作为第三版开发依据。
> 配套：《第二版内部需求文档.md》（历史）、《第二版用户需求文档.md》

---

## 1. 实体关系模型（本次确定，后续需求以此为准）

```
平台（系统管理员 SysAdmin，guanliyuan）——平台运营方，凭证只放微服务端
  └── 租户 Tenant（公司）——一家入驻公司 = 一个租户
        ├── 租户管理员 TENANT_ADMIN（老板/合伙人，可多个）
        │      · 属于公司（tenantId=租户），不属于任何家庭（customerId=空）
        │      · 权限：公司内全部（成员管理、家庭管理、田块设备增删、分配、任务）
        └── 客户 Customer（家庭）——一个购买/使用单位 = 一个客户，可多个
              ├── 客户用户 CUSTOMER_USER（家庭成员，可多个，平级无管理员）
              │      · 属于公司（tenantId=租户）也属于家庭（customerId=客户）
              │      · 权限：仅看/操作本家庭被分配的田块设备（可操作不可管理）
              └── 已分配田块（asset）/设备（device）——按家庭分配，家庭内成员共享
```

**核心规则（一句话）**：租户管理员属于公司不属于家庭；客户用户既属于公司也属于家庭；设备按家庭分配，家庭内成员共享。

**用户表归属字段**（TB 原生，已验证真实数据）：
| 账号类型 | tenant_id | customer_id |
|---|---|---|
| 租户管理员 | 所属租户 | 空 |
| 客户用户 | 所属租户 | 所属客户 |

---

## 2. 角色与权限矩阵

| 操作 | 租户管理员 | 客户用户（家庭成员） |
|---|---|---|
| 新增/删除成员（管理员或使用者） | ✅ | ❌ |
| 新建家庭 / 加入已有家庭 | ✅ | ❌ |
| 分配田块/设备给家庭 | ✅ | ❌ |
| 查看数据、开关设备（单控/批量）、定时任务 | ✅ 全部 | ✅ 仅本家庭分配的 |
| 删除设备/田块、挂载 | ✅ | ❌ |

安全由两层保证：**App 按角色隐藏入口**（isAdmin）+ **TB 原生 403**（CUSTOMER_USER 调管理接口被拒）。

---

## 3. 功能设计（App「我的 → 成员管理」）

### 3.1 成员列表
- 展示：头像位 + 邮箱 + 角色徽标（管理员/使用者）+ 所属家庭（使用者显示家庭名，管理员显示"公司"）
- 操作：删除成员（二次确认）

### 3.2 新增成员（核心改造：角色 + 归属二选一）

```
新增成员
 ├─ 角色单选
 │    ○ 管理员（公司老板/合伙人）
 │    ○ 使用者（家庭成员）
 ├─ 归属（角色=管理员时固定"加入本公司"；角色=使用者时二选一）
 │    ○ 新建家庭（填家庭名称 → 自动建客户）
 │    ○ 加入已有家庭（下拉选择：客户1 / AAA / ...）
 ├─ 邮箱（必填，唯一）
 └─ 确定 → 默认密码 123456 → 登记强制改密（首登改密）
```

### 3.3 删除（区分两种）
- **删除成员**：只删账号（`DELETE /api/user/{userId}`），家庭和其他成员、设备分配不受影响
- **删除家庭**：删整个客户（`DELETE /api/customer/{customerId}`），TB 级联删除该家庭下全部成员账号

### 3.4 分配可见范围（单位=家庭）
- 新建家庭后：勾选田块/设备分配给该家庭 → 家庭内所有成员共享
- 加入已有家庭：自动共享该家庭已分配的田块/设备（无需重复分配，可补充分配）

---

## 4. 接口设计（全部 TB 原生，微服务端零改动）

### 4.1 创建成员
```
角色=管理员（加入本公司）：
① POST /api/user {email, authority:"TENANT_ADMIN", tenantId:<本公司>}
② GET  /api/user/{id}/activationLinkInfo → 激活token
③ POST /api/noauth/activate {activateToken, password:"123456"}
④ 微服务端登记强制改密：POST /api/auth/mark-must-change {email}

角色=使用者-新建家庭：
① POST /api/customer {title:家庭名} → customerId
② POST /api/user {email, authority:"CUSTOMER_USER", tenantId, customerId} → userId
③ 激活 + 登记强制改密（同上）
④ 分配：POST /api/customer/{customerId}/asset/{assetId}  × N
        POST /api/customer/{customerId}/device/{deviceId}  × N

角色=使用者-加入已有家庭：
②' POST /api/user {email, authority:"CUSTOMER_USER", tenantId, customerId:<已有>} → userId
③' 激活 + 登记强制改密
```

### 4.2 删除
```
删除成员：DELETE /api/user/{userId}
删除家庭：DELETE /api/customer/{customerId}（级联删成员账号）
```

### 4.3 查询
```
成员列表：GET /api/customers → 每个客户 GET /api/customer/{cid}/users（成员+角色）
当前租户：GET /api/auth/user → tenantId（CurrentUserDto 已含）
```

---

## 5. 安全设计（重点，防止越权）

1. **"加入公司"不允许注册时自选**：新公司=注册（SysAdmin 代建，第一个老板）；额外管理员只能由**公司现有管理员在 App 内添加**（tenantId 取当前登录者所属租户，无法指定别家公司）→ 杜绝"注册挑一家公司当管理员"的越权
2. **加入家庭由创建者指定**：使用者归属哪个家庭由操作的管理员选择，用户自己不能选
3. **App 入口按角色隐藏** + **TB 原生 403** 双保险
4. 默认密码 123456 一律登记强制改密（管理员账号同样登记，首登必改）

---

## 6. 改动清单（文件级）

| 文件 | 改动 |
|---|---|
| `ThingsBoardApi.kt` | `createUser` 参数化（email/authority/tenantId/customerId）；新增 `deleteUser(userId)`；`getCustomerUsers` 已有 |
| `ThingsBoardRepository.kt` | 新增 `createMember(角色, 归属, 邮箱)` 编排（建客户/建用户/激活）；`loadMembers()` 返回 客户+成员+角色；`deleteUser(userId)`；`deleteCustomer` 保留 |
| `UserViewModel.kt` | `createMember`/`deleteMember`/`loadMembers`；成员列表状态（含角色徽标数据） |
| `UserManagementScreen.kt` | 成员列表（角色徽标+所属家庭）；新增成员弹窗（角色单选+归属选择+家庭下拉/新建）；删除确认区分"删成员/删家庭" |
| `MineScreen.kt` | 入口文案"使用者管理"→"成员管理" |
| `第二版内部需求文档.md` | 补第 2.5 节实体关系模型 + 成员管理功能（升级 v2.2） |
| 微服务端 task-service | **零改动**（mark-must-change 已存在） |

---

## 7. 实施步骤（每步可验证）

| 步骤 | 内容 | 验证 |
|---|---|---|
| 1 | 本文档模型确认（本方案） | 与用户对齐实体关系 |
| 2 | API 层：createUser 参数化 + deleteUser | gradle 编译通过 |
| 3 | Repository：createMember / loadMembers / deleteUser | 编译通过 |
| 4 | ViewModel：状态与方法 | 编译通过 |
| 5 | UI：成员管理页改造（列表+弹窗+删除区分） | 编译通过 + 模拟器 |
| 6 | 模拟器实测（见第 8 节场景清单） | 全场景通过 |
| 7 | 需求文档 v2.2 更新 + git 提交 | 文档核对 |

---

## 8. 验证场景清单

1. 管理员创建第二个管理员（新邮箱）→ 新管理员登录成功 → 看到全租户数据 → 首登强制改密
2. 管理员创建使用者选"加入客户1" → 与 15079983268 共享设备 → 登录后看到相同田块/设备
3. 管理员创建使用者选"新建家庭 BBB" → 新客户+新账号 → 给 BBB 分配田块/设备 → 登录可见
4. 删除成员（只删账号）→ 家庭其他成员不受影响、设备分配保留
5. 删除家庭 → 家庭内成员账号全部消失（TB 级联）
6. 使用者登录 → 无"成员管理"入口；强行调管理接口 → TB 403
7. 回归：原有员工（15079983268）登录、任务、田块设备管理均正常

---

## 9. 兼容性与回归

- 现有数据兼容：客户1（双成员）、AAA（单成员）改造后 App 列表直接可见，无需迁移
- 微服务端、模拟器、TB 配置零改动
- App 需重新打包安装（模拟器 + 真机 APK）

---

## 10. 不做的事（本期边界）

- 不做"注册时选公司"（安全风险，见第 5 节）
- 不做邀请码/审批流（有需要后续再加）
- 不做客户用户之间的数据隔离细分（家庭成员共享本家庭全部，符合预期）
