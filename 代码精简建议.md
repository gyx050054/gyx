# 智能灌溉系统 代码精简建议

> 版本：v1.0（2026-08-10）
> 依据：全量只读审查（task-service 16 个 Java 文件、kotlin-demo 38 个 Kotlin 文件、simulators 10 个 Python 文件全部通读），关键结论已抽查复核。
> 原则：所有精简均为**功能不变重构**，须遵守工程约束（中文注释、高内聚低耦合、改完编译验证）。

## 1. 代码量现状

| 部分 | 文件数 | 总行数 | 说明 |
|---|---|---|---|
| task-service | 16 个 .java | 1357 行 | 另含 application.yml 27 行、pom.xml 53 行 |
| kotlin-demo | 38 个 .kt | 约 7495 行 | 单文件最大：MainScreen.kt 1017 行、DeviceDialogs.kt 527 行 |
| simulators | 10 个 .py | 约 1383 行 | 另含 device_inventory.json、sensor1.log 等非源码文件 |

**结论：整体不过度复杂，但存在两类问题——重构残留死代码（App 近 1/5 代码）与少量复制粘贴。**

---

## 2. 死代码（删除零风险，收益最大）

| 位置 | 行数 | 说明 |
|---|---|---|
| `kotlin-demo/.../ui/screens/Screen1.kt` | 175 | 旧版页面，grep 证实无任何引用 |
| `kotlin-demo/.../ui/screens/Screen2.kt` | 560 | MainScreen 旧版（含大量与新版重复代码 + 未使用 import） |
| `kotlin-demo/.../ui/screens/Screen3.kt` | 283 | TaskManagementScreen 旧版 |
| `kotlin-demo/.../ui/screens/Screen4.kt` | 355 | FieldDetailScreen 旧版 |
| `kotlin-demo/.../data/repository/FarmRepository.kt` | 191 | 仅被 Screen1 引用（Screen1 本身是死代码）；内含 44 行与 `data/model/Device.kt:67-114` 逐行相同的 `toDevice`/`applyTelemetry` 旧副本 |

**小计：约 1564 行纯死代码，删后对编译/运行零影响。**

---

## 3. 失效配置（bug）

- `simulators/test_irrigation.py:33`：`SVC = "http://localhost:9091"`，实际端口已改 **9300**（task-service application.yml、AppConfig.kt:16 均为 9300）→ 该脚本任务链路测试（D 部分）全部连不上。**一行修复：9091 → 9300。**

---

## 4. 少量重复（可合并，各减 20~90 行）

### 4.1 task-service（Java）

| 位置 | 问题 | 建议 |
|---|---|---|
| `ThingsBoardClient.java` 与 `ThingsBoardAdminClient.java` | token 缓存字段 + `getToken` + `login`（38-90 vs 44-90）、构造器 RestTemplate 超时（50-57 vs 56-63）、HTTP 封装 postJson/baseHeaders（145-152 vs 177-194）高度雷同，约 90 行重复 | 抽公共基类或参数化构造（两者仅用户名/密码字段不同） |
| `AuthController.java:91-96` `ok()` 与 `TaskController.java:134-144` `ok()`/`error()` | 相同响应封装，各 new 一个 ObjectMapper（33/32） | 抽公共响应工具类 |
| `AuthService.java:92-101` `markPasswordChanged` 与 `108-117` `markMustChangePassword` | 仅差一个 true/false | 合并为一个带参方法 |
| `AuthController.java:46-53,72-74,84-86`、`TaskController.java:61-63,84-86` 手写 try/catch | 与 `GlobalExceptionHandler.java:26-33`（已统一处理 IllegalArgumentException→400）职责重叠 | 删除 Controller 内 catch |
| `TaskRepository.java:24` `findByStatusIn` | 全工程无调用 | 删除；27/30 行两个查询可合并为带条件查询 |
| `TaskController.createSingle(52-72)` 与 `createBatch(78-101)` | 参数解析 + 响应拼装两套逻辑重复 | 共用辅助函数 |

### 4.2 simulators（Python）

| 位置 | 问题 | 建议 |
|---|---|---|
| `tb_setup.py:22-24,31-39`、`tb_attributes_dashboard.py:25-38`、`provision_demo_tenant.py:36-55`、`test_irrigation.py:32-60` | 4 个 REST 脚本各自重复定义 `BASE/USERNAME/PASSWORD` + `login()` + `api()`，而 `config.py:32-34` 已有同值常量 `TB_BASE_URL/TB_USERNAME/TB_PASSWORD` | 收敛到 `config.py` 或新 `rest_common.py`（约减 60 行） |
| `test_irrigation.py:63-70` `load_inventory`/`find_device` | 与 `device_registry.py:13-29` 重复 | 改用 device_registry |
| `provision_demo_tenant.py:143-190` `build_demo_data` | 与 `tb_setup.py:166-198` 建模循环几乎逐行相同 | 提取公共建模函数（已复用 tb_setup 的部分函数，循环体仍复制） |

### 4.3 kotlin-demo（Kotlin）

| 位置 | 问题 | 建议 |
|---|---|---|
| `AuthRepository.kt:41`、`FarmRepository.kt:28`、`ThingsBoardRepository.kt:38` | 登录 `mapOf("username" to ..., "password" to ...)` 重复 3 次 | 抽公共请求体构造（FarmRepository 若删则剩 2 处，收益小） |
| `LoginScreen/RegisterScreen/ChangePasswordScreen/MineScreen` | 四个表单页结构几乎同模板（Spacer(48)+标题+Card+OutlinedTextField+Button+错误提示） | 可抽公共表单骨架（**注意：收益一般，可能过度抽象，建议保留现状**） |
| `TaskServiceApi.kt:17,35,43,47` `@Body body: Any` | 请求体类型不安全，map 在 Repository 层散落构造 | 改为具名 DTO（属健壮性改进，非精简） |
| `ChangePasswordScreen.kt:142`、`ThingsBoardRepository.kt:277` 默认密码 "123456" 字面量 | 未统一引用 `AuthService.DEFAULT_PASSWORD` | 常量统一（收益小） |

---

## 5. 不建议动的地方（避免过度精简）

- **`TaskServiceApi` 与 `ThingsBoardApi` 不合并**：端点职责不同（微服务端 register/mustChangePassword/pwdChanged vs TB login/changePassword）。
- **模拟器 `mqtt_base.py`**：MQTT 连接/重连/订阅/上报已全部收敛到基类（28-129），sensor/valve 均继承复用，**已重构得很好**，无重复。
- **`TaskExecutor` 接口 + `RpcTaskExecutor`（75 行）**：单实现，但按"为中间件/高并发铺垫"的工程约束刻意保留，建议不动。

---

## 6. 建议执行顺序（按收益/风险）

| 优先级 | 内容 | 预计减行 | 风险 |
|---|---|---|---|
| P0 | 删除 Screen1-4 + FarmRepository（含旧 toDevice/applyTelemetry 副本） | ≈1564 行 | 零（死代码） |
| P0 | 修 `test_irrigation.py:33` 端口 9091→9300 | 0（bug 修复） | 零 |
| P1 | simulators 抽公共 REST 模块 + 复用 device_registry + 提取建模循环 | ≈60 行 | 低（改后跑一遍 provision 验证） |
| P1 | Java：合并 ThingsBoardClient/AdminClient、Controller 去重、删死方法 | ≈110 行 | 低（mvn compile + 启动验证） |
| P2 | Kotlin 登录 body 抽公共、默认密码常量统一 | ≈10 行 | 低（gradle compile） |
| 不做 | 表单页抽公共骨架（可能过度抽象）、TaskExecutor 内联 | — | — |

---

## 7. 备注

- 全部为**功能不变重构**，每步改完须编译验证（task-service 用 `D:\maven\apache-maven-3.8.1\bin\mvn.cmd compile`，App 用 `gradlew.bat compileDebugKotlin`，simulators 用 `py -m py_compile`）。
- 本文档由代码审查生成，**未执行任何代码改动**。
