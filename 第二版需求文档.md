# 智能灌溉系统 第二版需求文档（完整版）

---

## 一、第一版功能回顾

第一版是一个**单账号**的灌溉管理 App：
- 只有一个账号（管理员/农户合一），登录后看田块、看设备、开关阀、设定时任务
- 田块、设备全部由**后台脚本预置**（App 里不能自己建）
- 任务表全局共享，不做任何隔离
- 登录状态不保存（关 App 就掉线）
- 无注册、无角色、无强制改密、无设备凭证、无成员管理

---

## 二、第二版改动总览

| 类别 | 改动项 |
|---|---|
| **新增** | 租户注册、多租户与双角色、田块增删、设备增删+挂载、设备凭证、成员管理（管理员/家庭成员）、任务冲突清理交互、任务排序与状态展示、任务红点、我的页、首次登录强制改密、常见问题页、预置演示账号、定制 Logo |
| **修改** | 任务按租户隔离、查询接口按角色切换、切换账号缓存清理、任务管理页交互、新增设备流程（自由设备）、删除区分成员/家庭 |
| **沿用** | 田块总览、田块详情、历史曲线、单控/批量开关、10 秒刷新、定时任务四态、阀门卡片 UI |

---

## 三、逐模块详细改动（第一版 → 第二版）

### 3.1 登录

| # | 第一版 | 第二版 | 为什么 |
|---|---|---|---|
| 1 | 登录页只有"账号+密码+登录" | 新增 **「没有账号？注册」链接**（→ 3.2） | 第二版支持用户自助注册，入口放在登录页 |
| 2 | 登录页无帮助 | 新增 **「常见问题」入口**，静态文案页 | 引导新手理解"注册=开公司、客户用户=家庭成员"等概念 |
| 3 | 登录后直接进主界面 | 登录成功先调 `must-change-password` 查**是否需强制改密**，需要则先进改密页 | 支持"首次登录强制改密"（见 3.3） |
| 5 | 单账号 | 登录后自动识别身份：**租户管理员 / 客户用户（家庭成员）**，界面按角色显示（见 3.4） | 多租户双角色架构 |

### 3.2 注册

| # | 第一版 | 第二版 | 为什么 |
|---|---|---|---|
| 1 | 无注册功能 | 登录页点"注册"→ 填**邮箱**（+手机号预留）→ 提交到**微服务端** | TB 无自助注册，必须由微服务端以系统管理员身份代建 |
| 2 | — | 注册成功 = **自动创建 1 个租户（公司）+ 1 个租户管理员账号（第一个老板）**，默认密码 `123456` | 用户视角"注册即开公司、我即老板" |
| 3 | — | 注册成功**自动登录**（App 调 TB login） | 减少一步，注册完直接进系统 |
| 4 | — | 注册账号**登记强制改密**（user_pwd_flag 表），首次登录进改密页 | 默认密码是弱口令，强制改密降低撞库风险 |
| 5 | — | 邮箱格式校验、重复邮箱拒绝、失败有明确提示 | 基础表单校验 |

### 3.3 首次登录强制改密

| # | 第一版 | 第二版 | 为什么 |
|---|---|---|---|
| 1 | 无改密流程 | 所有新账号（注册/成员/预置）首次登录**必须先改密**才能进主界面 | 默认密码 `123456` 必须替换 |
| 2 | — | 改密页"当前密码"字段**自动代填默认密码（App 代填）**，用户只需输入新密码+确认 | TB 的改密接口必须校验当前密码；App 代填实现"不需要旧密码"的体验 |
| 3 | — | 改密成功后调 `pwd-changed` 清除强制改密标记 | 下次登录直接进主界面 |
| 4 | — | **预置演示账号 `15079983758` 不登记改密标记**（查无记录=无需改密），老账号不受影响 | 实测确认：只有走注册/成员接口新建的账号才需要强制改密 |

### 3.4 角色与权限（新增）

| # | 第一版 | 第二版 | 为什么 |
|---|---|---|---|
| 1 | 无角色概念 | 引入三种身份：**系统管理员 / 租户管理员 / 客户用户（家庭成员）** | TB 原生四层模型的落地 |
| 2 | — | 权限矩阵：租户管理员=全部（田块/设备增删、成员管理、分配）；客户用户=只可操作（看数据、开关阀、定时任务），**不可管理** | "老板管、员工干"的产品定位 |
| 3 | — | **查询接口按角色切换**：管理员走 `/api/tenant/deviceInfos`、`assetInfos`；客户用户走 `/api/customer/{id}/deviceInfos`、`assetInfos` | TB 权限模型：员工只能看到被分配的田块/设备，必须走客户视角接口（否则 403 看不到数据） |
| 4 | — | **管理按钮按角色隐藏**：新增/删除田块、新增设备、挂载、删除设备、成员管理入口，仅租户管理员可见 | 双保险（App 隐藏 + TB 原生 403） |
| 5 | — | 越权兜底：客户用户强行调管理接口 → TB 返回 **403** | TB 原生安全，无需自写权限判断 |

### 3.5 田块管理（租户管理员）

| # | 第一版 | 第二版 | 为什么 |
|---|---|---|---|
| 1 | 田块由脚本预置 | 田块页右上角 **「+」新增田块**，弹窗输入名称 → `POST /api/asset`（FIELD profile） | 自助建田块 |
| 2 | 无删除 | **删除田块**：点「删除田块」按钮 → 进入**选择模式**（点田块卡片可多选，选中高亮）→ 二次确认 | "点卡片选择模式"是用户明确要求的交互；多选一次删多块 |
| 3 | — | 删除确认弹窗文案：「将删除选中的 N 块田块，其下设备将变为自由设备（未挂载），是否继续？」 | 明确告知"设备不删、只解绑" |
| 4 | — | 删除后**设备变为自由设备**（保留，可重新挂载） | 田块是容器，设备是资产，资产不随容器删除 |
| 5 | — | 员工（客户用户）**看不到新增/删除田块按钮** | 权限隔离 |

### 3.6 设备管理（租户管理员）

| # | 第一版 | 第二版 | 为什么 |
|---|---|---|---|
| 1 | 设备由脚本预置 | 「所有设备信息」页顶部**「新增设备」**按钮 | 自助建设备 |
| 2 | — | 新增设备弹窗：**设备名称输入框** + **类型单选（仅：电动阀 / 温度湿度计）** | **这个单选框的作用**：决定设备用哪个 TB 设备配置（profile）、App 里怎么展示（阀门可控制/温湿度计不可控制）、上报哪些数据字段 |
| 3 | — | **新增设备不再选田块** → 新增后是**自由设备**（未挂载） | 与 TB 模型一致：设备先建，后挂载；避免"建的时候就绑死田块" |
| 4 | — | 新增成功后**弹凭证框**：展示 Access Token + 复制按钮 + 提示"配置到设备后数据自动出现" | 真设备接入：用户拿 Token 去配置硬件 |
| 5 | — | **两种挂载方式**：① 田块详情页「挂载」→ 弹窗列出**自由设备**勾选挂入；② 设备页每台自由设备提供「挂载」→ 弹窗**选田块**下拉 | 用户在不同入口都能完成"设备归田块" |
| 6 | — | **删除设备**：二次确认「将删除设备「X」，其未完成的定时任务将被取消，且不可恢复」→ 先调微服务端 `DELETE /api/tasks/device/{deviceId}` 取消任务 → 再删 TB 设备 | 删除联动：不留孤儿任务 |
| 7 | — | 员工**看不到新增/挂载/删除设备**按钮 | 权限隔离 |
| 8 | — | 已挂载设备不显示"挂载"按钮（按 fieldId 区分自由/已挂载） | 避免重复挂载 |

### 3.7 成员管理（v2.2 新增，替代"农户管理"）

**概念模型（重要，与第一版完全不同）：**
```
公司（租户）——租户管理员（老板，可多个，属于公司不属于家庭）
   └── 家庭（客户）——可多个
         ├── 客户用户（家庭成员，可多个，平级无管理员）
         └── 已分配田块/设备（按家庭分配，成员共享）
```

| # | 第一版 | 第二版 | 为什么 |
|---|---|---|---|
| 1 | 无任何成员概念 | 「我的 → 成员管理」（仅租户管理员可见） | 管理"谁在用这个系统" |
| 2 | — | 成员列表 = **家庭卡片**（🏠 家庭名 + N 名成员）+ 每个成员行（邮箱 + **"家庭成员"角色徽标** + 删除按钮） | 按家庭组织，成员共享同一批设备 |
| 3 | — | 页面顶部**「🏢 本公司（N 名管理员）」卡片**：列出公司全部管理员邮箱，当前登录者标 **"👑 xxx（我）"** | 用户需求：看清"公司有哪些老板"；管理员属于公司不属于家庭，单独展示 |
| 4 | — | **新增成员弹窗**包含：
  - **角色单选（两个 chip）**："使用者（家庭成员）" / "管理员（公司合伙人）"——**作用**：决定账号挂到家庭（customerId）还是公司（tenantId 且无 customerId），决定权限大小
  - **归属选择（两个 chip）**："新建家庭" / "加入已有家庭"——**作用**：使用者必须属于某个家庭；新建=先建客户再挂账号，加入=直接挂到已有客户（成员共享该家庭设备）
  - 选"管理员"时显示提示文案"管理员将加入本公司，可管理全部田块/设备/成员"
  - 选"新建家庭"显示**家庭名称输入框**；选"加入已有家庭"显示**家庭单选列表**（Public 默认客户已过滤）
  - **邮箱输入框** + 提示"初始密码默认 123456，首次登录需修改密码" | 用户需求：一个家庭多人共用一套设备 |
| 5 | — | **删除区分**：删成员（只删账号 `DELETE /api/user/{id}`，家庭/设备/其他成员不受影响）vs 删家庭（删客户，**级联删该家庭所有成员账号**，田块/设备/任务保留） | 用户需求：删"人"和删"家"是两件事 |
| 6 | — | **分配可见范围**（按家庭）：勾选田块/设备 → 分配给家庭 → **家庭内所有成员共享** | 设备按家庭分配，成员自动继承 |
| 7 | — | 创建成员成功 → **登记强制改密**（mark-must-change） | 新成员默认密码首登必改 |
| 8 | — | 创建管理员也用同一入口（角色选"管理员"），tenantId **自动取当前公司**，无法指向别家公司 | 安全：谁进公司由公司现有管理员说了算，杜绝越权 |

### 3.8 任务模块（多处修改 + 新增交互）

| # | 第一版 | 第二版 | 为什么 |
|---|---|---|---|
| 1 | 任务全局共享 | **任务按租户隔离**：创建/查询都带当前租户 id（tenantId）；各公司只见自己的任务 | 用户发现：公司2 能看到公司1 的任务 → 修复 |
| 2 | — | **任务列表排序**（用户指出不合理）：**执行中(RUNNING)最上 → 未开始(PENDING) → 已完成/已取消沉底**，同状态按开始时间倒序 | 用户需求 |
| 3 | — | **任务状态展示**：每个任务卡片显示状态点 + 状态文字 + 状态颜色（🟢执行中/⏳等待/✅已完成/🚫已取消）+ 卡片背景色区分 | 用户需求：状态要"展现出来"（第一版卡片已有，第二版强化排序后更直观） |
| 4 | — | **任务卡片勾选框**（仅 PENDING/RUNNING 可勾选）+ 顶部**「全选未完成任务」按钮** + **「停止所选(N)」一键停止按钮** + 已选数量提示（"共 N 条任务 · 已选 M 条"） | 用户需求：批量清理任务 |
| 5 | — | **冲突清理交互**（新增弹窗）：创建任务失败且因冲突 → 弹窗 **「存在冲突的任务」：「设备『X』此时段已有任务进行中，是否清除该设备的任务？」** → 点「去清理」→ 自动跳任务管理页并**预勾选该设备全部未完成任务** → 用户点「停止所选」一键停止 | 用户需求：冲突时引导用户清理，而不是只给一句失败提示 |
| 6 | — | 批量创建冲突时（无法定位单设备）→ 弹窗文案为"所选设备"，去清理后**全选未完成任务** | 批量场景无法定位到具体哪台 |
| 7 | 任务图标带数字 Badge | **任务红点**：有任务且未访问任务页时显示红点，**访问后消失**，退出登录重置 | 用户需求：已读/未读 |
| 8 | 执行中任务取消=先发暂停再取消 | 沿用（PENDING 直接取消 / RUNNING 先 pauseValve 再取消，软删除 CANCELLED） | 不变 |

### 3.9 我的页（新增）

| # | 第一版 | 第二版 | 为什么 |
|---|---|---|---|
| 1 | 无"我的"概念 | 底部导航新增**第三个 Tab「我的」** | 承载身份、成员管理、退出 |
| 2 | — | 显示**当前身份**：邮箱 + 角色（"租户管理员（老板）" / "员工（使用者）"） | 用户明确知道自己是谁 |
| 3 | — | **「成员管理」入口**（仅租户管理员可见） | 管理成员 |
| 4 | — | **「退出登录」按钮** | 换账号 |

### 3.10 切换账号数据隔离（第三版修复）

| # | 第一版 | 第二版 | 为什么 |
|---|---|---|---|
| 1 | 退出只跳登录页 | **退出登录时统一清空三个 ViewModel**（田块/设备/任务/成员/身份）+ **清 JWT** + **清 Repository 缓存（tenantId/角色）** | 用户发现：偶发"退出后登录别的账号，数据还是上一个账号的"——根因是 Repository 实例缓存了上个账号的租户 id |

### 3.11 UI / Logo

| # | 第一版 | 第二版 | 为什么 |
|---|---|---|---|
| 1 | 默认图标 | **定制应用图标**（紫色水滴 + 灌溉波浪，5 种分辨率） | 用户需求：Logo 好看 |
| 2 | 默认主题 | Material3 紫色主题统一 | 用户需求：大气 |
| 3 | 阀门卡片普通 | **大号开关 + 数据行（瞬时流量/水压/累计用水/电量）**；操作收进「更多」菜单 | 用户需求（用户需求文档 3.8） |

### 3.12 微服务端（后端新增）

| # | 第一版 | 第二版 | 为什么 |
|---|---|---|---|
| 1 | 无注册接口 | `POST /api/auth/register`：SysAdmin 建租户+管理员+默认密码激活 | TB 无自助注册 |
| 2 | 无改密标记 | `user_pwd_flag` 表 + `GET must-change-password` + `POST pwd-changed` + `POST mark-must-change` | 强制改密标记 |
| 3 | 任务无租户字段 | `tasks` 表加 `tenant_id`（String，存 TB 租户 UUID）+ 查询按租户过滤 | 任务租户隔离 |
| 4 | 无删除设备接口 | `DELETE /api/tasks/device/{deviceId}`：删除设备时级联取消其未完成任务 | 设备删除联动 |

### 3.13 模拟器与预置账号

| # | 第一版 | 第二版 | 为什么 |
|---|---|---|---|
| 1 | 模拟器直连本机 IP | `config.py` HOST 改 `localhost`（TB 本地 Docker 1883） | 本机 IP 会变导致连不上 |
| 2 | — | 预置演示账号 `15079983758@163.com / 147258`：9 田块 + 27 台模拟器设备 | 演示/验收用，登录即见全部数据 |
| 3 | — | `provision_demo_tenant.py`：SysAdmin 建演示租户+管理员+9 田块 27 设备（幂等） | 一键重建演示数据 |
| 4 | — | **App 新增设备不自动产生模拟器进程**（预期行为，文档说明） | 模拟器是脚本预置的 |

---

## 四、与第一版差异明细表（含每个小点）

| # | 功能点 | 第一版 | 第二版 | 类型 |
|---|---|---|---|---|
| 1 | 注册入口 | 无 | 登录页"注册"链接 | 新增 |
| 2 | 租户注册 | 无 | 微服务端代建租户+管理员，默认密码 123456，自动登录 | 新增 |
| 3 | 强制改密 | 无 | 首登必改；改密页当前密码 App 代填；user_pwd_flag 标记 | 新增 |
| 5 | 常见问题页 | 无 | 登录页入口，静态文案 | 新增 |
| 6 | 角色 | 单角色 | 租户管理员/客户用户双角色 | 新增 |
| 7 | 查询接口按角色 | 统一 tenant 接口 | 员工走 customer 接口 | 修改 |
| 8 | 管理按钮按角色 | 全部可见 | 员工隐藏管理入口 | 修改 |
| 9 | 新增田块 | 无 | 右上角 +，弹窗输入名称 | 新增 |
| 10 | 删除田块 | 无 | 选择模式多选 + 二次确认 + 设备变自由 | 新增 |
| 11 | 新增设备 | 无 | 名称 + 类型单选（自由设备）+ 凭证弹窗 | 新增 |
| 12 | 设备挂载 | 无 | 两种方式（田块详情挂/设备页选田块） | 新增 |
| 13 | 删除设备 | 无 | 二次确认 + 联动取消任务 | 新增 |
| 14 | 设备凭证 | 无 | Access Token 展示+复制 | 新增 |
| 15 | 成员管理 | 无 | 家庭卡片+成员+角色徽标+公司管理员展示 | 新增 |
| 16 | 新增成员弹窗 | 无 | 角色单选 chip + 归属选择 chip + 家庭名/家庭列表 + 邮箱 | 新增 |
| 17 | 删除区分 | 无 | 删成员（只删账号）/删家庭（级联） | 新增 |
| 18 | 分配可见范围 | 无 | 按家庭勾选田块/设备，成员共享 | 新增 |
| 19 | 任务租户隔离 | 全局共享 | 创建/查询带 tenantId | 修改 |
| 20 | 任务排序 | 按 id | 执行中→未开始→已完成/取消 | 修改 |
| 21 | 任务状态展示 | 有文字 | 强化（点+文字+色+卡片底色） | 修改 |
| 22 | 任务勾选/一键停止 | 无 | 勾选框+全选+停止所选 | 新增 |
| 23 | 冲突清理交互 | 只提示失败 | 弹窗"是否清除"→跳任务页预勾选 | 新增 |
| 24 | 任务红点 | 数字 Badge | 红点访问后消失 | 修改 |
| 25 | 我的页 | 无 | 身份+成员管理入口+退出 | 新增 |
| 26 | 退出清理 | 只跳页 | 清 JWT+三个 ViewModel+缓存 | 修改 |
| 27 | 应用图标 | 默认 | 定制紫色水滴 | 新增 |
| 28 | 主题 | 默认 | Material3 紫色 | 修改 |
| 29 | 微服务端注册 | 无 | POST /api/auth/register | 新增 |
| 30 | 微服务端改密标记 | 无 | user_pwd_flag + 3 个接口 | 新增 |
| 31 | 任务表租户字段 | 无 tenant_id | tasks.tenant_id (String) | 修改 |
| 32 | 删除设备任务接口 | 无 | DELETE /api/tasks/device/{deviceId} | 新增 |
| 33 | 预置演示账号 | 无 | 15079983758 / 147258 + 9 田块 27 设备 | 新增 |
| 34 | 模拟器连接 | 本机 IP | localhost（TB Docker） | 修改 |

---

## 五、沿用不变（第一版已有，第二版未改）

- 温湿度计/电动阀的数据上报格式与 MQTT 主题（v1.2 设备端运行规则）
- 田块总览卡片（名称+设备数）
- 田块详情设备列表、10 秒自动刷新
- 历史温度/湿度曲线（近 1 小时/24 小时/7 天/自定义）
- 单设备开关、批量开关（多选+一键全开/全关）
- 定时任务四态（PENDING/RUNNING/COMPLETED/CANCELLED）与扫描执行（每 10 秒）
- 任务冲突检测规则（时间段交集即冲突）
- 软删除（CANCELLED 保留记录）

---

## 六、实现状态标注

- ✅ 已实现并模拟器实测：本文档 3.1~3.13 全部功能
- 📌 文档说明：TB 默认客户 Public 已在成员管理中过滤；任务冲突逻辑为"重叠即拒"（如需"包含时允许"需另定需求）
