# 智能灌溉系统 第二版设计方案

> 版本：v2.0 草案（2026-08-05）
> 核心目标：**让国内低小成本的用户更便捷地使用 ThingsBoard**——降低 TB 的使用与学习成本，把"租户管理、田块管理、设备管理、设备接入"全部包装成傻瓜式操作。
> 本文档所有 TB 能力均经官方文档/源码查证（见文末"依据"）。

---

## 1. 产品定位

ThingsBoard 功能极其强大，但对普通用户门槛高：英文界面、专业概念（Tenant/Customer/Asset/DeviceProfile/Rule Chain）、配置繁琐。本系统定位为 **"TB 友好外壳 + 轻量管理平台"**：

- 目标用户：国内低小成本的农户、小型种植基地
- 卖点：注册即用、中文界面、田块/设备可视化管理、不用懂 TB 任何概念
- 架构原则：**能直连 TB 用原生 API，绝不重复造轮子；只在 TB 做不到的地方加自己的服务**

对标结论（详见《物联网灌溉系统第一代第三版/竞品分析.md》）：多人/多租户、田块设备管理是市场标配（Rachio 多人共享、CropX 多租户），本版补齐后与市场对齐。

---

## 2. 多租户模型（TB 原生映射）

```
平台运营者 SysAdmin（admin@thingsboard.org，就是系统所有者）
└── 租户A（Tenant A）            ← 用户注册时自动创建
    └── 租户管理员A（Tenant Admin） ← 注册账号本人
        ├── 管理本租户：田块/设备/农户账号/任务（全部操作被 TB 隔离在本租户内）
        └── 建农户账号（Customer 用户）← App端"租户登录"
└── 租户B（同理，与 A 完全隔离，互不可见）
```

**关键事实（已查证）**：
- TB 原生多租户，官方文档明确 "Each tenant owns and manages its own devices, assets, dashboards, users... fully isolated from all other tenants"——**租户隔离天然成立，无需自己实现**。
- 租户管理员只能操作本租户实体，数据不会串。

**术语对照（App 内用中文，背后是 TB 概念）**：

| App 里的说法 | TB 概念 | 说明 |
|------------|---------|------|
| 租户管理员 | Tenant Administrator | 注册账号本人，管理整个租户 |
| 租户/农户 | Customer User | 租户管理员在 App 里创建的子账号 |
| 田块 | Asset（type=FIELD） | 现有脚本已在用 |
| 设备挂载田块 | Relation（type=Contains） | 现有脚本已在用 |
| 设备 | Device（DeviceProfile: VALVE / TEMPERATURE_HUMIDITY） | 现有脚本已在用 |

---

## 3. 功能需求

### 3.1 注册（唯一不能 APP 直连 TB 的功能）

**重要现实约束（已查证）**：TB Community Edition **4.x 没有 `/api/noauth/register` 端点**（3.x 时代有，4.x 已移除；自注册是 PE/Cloud 专属）。因此：

- ✅ **登录**：APP 直连 TB `POST /api/auth/login`（保持现有架构）
- ❌ **注册**：不能 APP 直连，必须由持有 **SysAdmin 凭证**的服务创建租户

**实现**：微服务端（task-service）扩展注册接口

```
POST /api/auth/register
body: { "username": "...", "password": "..." }
→ 微服务端用配置中的 SysAdmin token 调 TB：
    1. POST /api/tenant           创建租户（title=用户名）
    2. POST /api/user?tenantId=.. 创建租户管理员（authority=TENANT_ADMIN）
→ 返回 { success, message }
→ App 提示"注册成功，请登录"（登录仍走 TB /api/auth/login）
```

**安全要求**：
- SysAdmin 凭证放微服务端配置文件（不走 App）
- **必须修改 TB 默认 admin 密码**（admin@thingsboard.org / sysadmin 默认密码全网公开，暴露风险极高）
- 注册接口要防滥用：验证码、邮箱/手机号校验、频率限制（第二版可先做简单频率限制）

### 3.2 登录（App 保持直连 TB）

- 登录页增加"租户管理员登录"和"租户登录"两个概念，但**底层都是 `POST /api/auth/login`**，无需区分——TB 按账号角色自动返回对应权限的 JWT
- **必须修**：现在 JWT 只存内存（`AuthInterceptor.kt` 静态变量），重启即掉线。第二版改用 SharedPreferences/DataStore 持久化，并区分三种角色（SysAdmin 不开放给 App、TENANT_ADMIN、CUSTOMER_USER）

### 3.3 田块管理（Tenant Admin 专属）

| 操作 | TB API | 说明 |
|------|--------|------|
| 新增田块 | `POST /api/asset`（type=FIELD） | App 输入田块名即可 |
| 删除田块 | `DELETE /api/asset/{id}` | 删前必须处理田块下设备（解绑或提示） |
| 田块列表 | `GET /api/tenant/assetInfos`（租户管理员）/ `GET /api/customer/{id}/assets`（农户） | 现有接口 |

### 3.4 设备管理（Tenant Admin 专属）

| 操作 | TB API | 说明 |
|------|--------|------|
| 新增设备 | `POST /api/device`（指定 deviceProfileId） | 类型仅限：电动阀(VALVE)、温度湿度计(TEMPERATURE_HUMIDITY) |
| 挂载田块 | `POST /api/relation`（from=ASSET, to=DEVICE, type=Contains） | App 选择"挂载到哪块田地" |
| 删除设备 | `DELETE /api/device/{id}` | **删前取消该设备的未完成任务（微服务端）** |
| 获取凭证 | `GET /api/device/{id}/credentials` | 拿到 accessToken 展示给用户（真设备接入用） |
| 设备列表 | `GET /api/tenant/deviceInfos` | 现有接口 |

**设备类型限制**：第二版只支持电动阀、温度湿度计两种——设备类型在 TB 里由 **DeviceProfile** 决定（VALVE / TEMPERATURE_HUMIDITY，现有脚本已建好）。App 新增设备时只给这两个选项，就是"类型限制"的天然实现。

### 3.5 真设备接入（三种模式）

模拟器与真设备协议完全一致（MQTT：username=access token、password 留空、`v1/devices/me/telemetry` 上报、`v1/devices/me/rpc/request/+` 收指令）→ **真设备接入服务端零改动**。

硬件：能跑 MQTT 即可——ESP32/ESP8266（WiFi，成本 ¥20-30）、STM32+4G DTU、树莓派。

| 模式 | 流程 | 适合 |
|------|------|------|
| **A. App 创建+手动录入/扫码**（第二版做这个） | App 建设备 → TB 返回 accessToken → App 展示 token/二维码 → 用户把 token 写进设备配置 | 低小成本用户，先做 |
| **B. 设备自动注册 Provisioning**（官方机制，已查证） | 设备出厂预置 provisionDeviceKey/Secret → 首次上电发 `provision/request` → TB 自动建设备并发回 token | 批量量产 |
| **C. 固件烧录** | 开发时直接把 token 写死进固件 | 自己测试 |

**"App 创建的设备能否直接连 TB？"——能。** TB 里的设备本质就是 access token 凭证，与谁创建无关：

```
App（租户管理员 token）
  → POST /api/device 创建设备
  → GET /api/device/{id}/credentials 拿 accessToken
  → App 展示 token/二维码 → 用户配进真设备
  → 真设备 MQTT 连 TB（username=token）→ 上报数据
```

**注意现实细节**：创建设备只是"登记"，真设备还需要有人把它物理装到地里并配置 token。所以"新增设备"功能必须带 **token 展示/导出环节**，否则设备拿不到任何凭证。

---

## 4. 改动清单

### 4.1 微服务端（task-service）——改动最小但最关键

1. **注册接口** `POST /api/auth/register`：SysAdmin token 调 TB 建 Tenant + Tenant Admin（如上 3.1）
2. **任务表加租户维度**：`task_schedule` 加 `tenantId`（及可选 `userId`）字段。现在 `GET /api/tasks` 返回全局所有任务，多租户后必须按租户过滤，否则 A 租户能取消 B 租户的任务
3. **删除设备的任务联动**：`DELETE` 设备时先取消其 PENDING/RUNNING 任务
4. （可选）注册频率限制

### 4.2 APP（kotlin-demo）——改动最多

1. 登录页加"注册"入口（注册走微服务端；登录仍直连 TB）
2. JWT 持久化（SharedPreferences/DataStore），重启不掉线
3. 登录态角色区分：
   - **TENANT_ADMIN**：田块/设备管理、农户账号管理、任务管理、数据查看
   - **CUSTOMER_USER**：只看被分配的田块/设备/仪表板，隐藏管理功能
   - **接口权限差异**：现在查设备用 `/api/tenant/deviceInfos`（租户级），Customer 用户调用会 403，需按角色切换（如 `/api/customer/{id}/deviceInfos` 或 relation 查询）
4. 田块管理页：新增/删除田块
5. 设备管理页：新增/删除设备、选择挂载田块、选择类型（仅电动阀/温湿度计）、**展示 accessToken/二维码**
6. 设备删除时提示"将取消其所有未完成任务"

### 4.3 ThingsBoard 侧——基本零改动

- 田块（Asset）、设备（Device）、关系（Relation）、租户隔离全是原生 API，现有脚本（tb_setup.py）逻辑可直接复用
- 唯一动作：**改默认 admin 密码**

---

## 5. 风险与注意事项（实事求是）

| 风险 | 说明 | 对策 |
|------|------|------|
| **SysAdmin 凭证泄露** | 注册服务持有平台最高权限 | 凭证只放服务端配置；改默认密码；注册接口做限流/验证码 |
| **注册滥用** | 开放注册会招垃圾账号 | 第二版先做频率限制，验证码后续 |
| **删除数据孤儿** | 删田块/设备留下悬空关系、未完成任务 | 删除前级联处理（解绑、取消任务） |
| **Customer 接口 403** | 农户账号调用租户级接口会失败 | App 按角色切换 API 路径 |
| **JWT 掉线** | 现在 token 不持久化 | 第二版必修 |
| **磁盘增长** | 时序数据只增不减 | 远期在 TB 配数据保留策略（TTL），第二版不受影响 |

**ThingsBoard 承载能力（回答"设备太多会不会接收不过来"）**：
- 官方 AWS benchmark：单机 Docker+PostgreSQL（本系统形态）稳定支撑 **10 万设备**、111 msg/s（低配实例 CPU 仅 5%），压测上限 1,111 msg/s
- 本系统最坏负载：18 阀全工作每 10 秒 1 条 ≈ **1.8 msg/s**，与官方基准差 60 倍
- TB 默认**禁用速率限制**（官方文档明示），不会因设备多拒收
- 需要升级的信号：设备过万或数据点 >5K/s，届时按官方路径 PostgreSQL→Cassandra/TimescaleDB→Kafka→微服务集群。**第二版完全不触及**

---

## 6. 第二版路线图（建议顺序）

```
第 1 步（核心，微服务端+TB）：注册接口 + 改 admin 密码 + 任务表加租户隔离
第 2 步（APP 基础）：JWT 持久化 + 登录页注册入口 + 角色区分
第 3 步（APP 管理）：田块新增/删除 + 设备新增/删除/挂载田块 + token 展示
第 4 步（联调）：多租户端到端验证（注册→建田块→建设备→挂载→模拟器接入→任务隔离）
第 5 步（真设备，可选）：ESP32 示例固件 + 扫码配网
```

---

## 7. 依据（已查证来源）

- TB 多租户与隔离：https://thingsboard.io/docs/user-guide/multi-tenancy/
- TB 自注册为 PE/Cloud 专属（CE 4.x 无 register 端点）：https://thingsboard.io/docs/user-guide/security/self-registration/ + master 源码 AuthController.java
- 设备自动注册（Provisioning）：https://thingsboard.io/docs/user-guide/provisioning/ 、https://thingsboard.io/docs/reference/mqtt-api/provisioning/
- MQTT 接入（token 作 username）：https://thingsboard.io/docs/reference/mqtt-api/getting-connected/
- Asset/Relation/Customer/Tenant/User REST API（controller 级确认）：https://thingsboard.io/docs/reference/rest-api/
- 性能基准与速率限制默认关闭：https://thingsboard.io/docs/reference/architecture/performance/ 、https://thingsboard.io/docs/reference/architecture/
