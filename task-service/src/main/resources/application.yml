server:
  port: 9300

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/task_service?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: "147258"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

# ThingsBoard 连接配置（按实际环境修改）
thingsboard:
  base-url: http://localhost:8080
  username: 15079983758@163.com
  password: "147258"
  rpc-timeout-ms: 10000

logging:
  level:
    com.irrigation.task: INFO
